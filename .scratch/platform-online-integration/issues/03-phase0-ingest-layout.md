> ## ⛔ 范围外（2026-08-18 合并裁决）
>
> 用户裁定 **Phase 0 不作交付**：不做脚本上传对接、cron、manifest 规范——那些是 Phase 1 上线后要扔的中间资产。三个 Python 脚本原样保留供人工兜底，但不投入工程化，也**不作为承诺的降级链**。
>
> 兜底 = **人工去平台导表 + 现有上传入口**（这条入口永在）。这同时采纳了红队 §2 的推翻结论：不维护「同一段契约双实现」的长期负债。
>
> 本票保留存档，不执行。
# 03 — Phase 0 拉取落盘对齐（彩食鲜 + 飞象）

**What to build:** 现有两个 Python 拉取脚本（彩食鲜 export 模式、飞象直下）对齐统一落盘规范：写入独立 ingest 目录（gitignored），文件名 `{渠道}-{业务}-{yyyyMMdd}-NN.{ext}`，每个文件旁写 manifest 边车（来源批次标识、拉取时间、行数、内容哈希）；脚本可重复执行且落盘命名幂等（同批次同日不重复覆盖）。

**Blocked by:** None — can start immediately

**Status:** wontfix

- [ ] 彩食鲜/飞象脚本输出到 ingest 目录，命名与 manifest 符合规范
- [ ] 同一批次重复运行不产生重复文件（同日同批次复用）
- [ ] 输出样例（彩食鲜 22 列、飞象 21 列）与规范指纹一致——飞象需先用真实区间数据验证数据行（当前样例仅表头）
- [ ] ingest/ 已在 .gitignore 内
