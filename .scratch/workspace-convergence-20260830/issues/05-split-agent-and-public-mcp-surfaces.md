# 05: 统一 Agent 与公共 MCP 双工具面

**What to build:** 工具注册表向 Agent 调用者和公共 MCP 调用者提供两套明确、不可混淆的工具面；新增业务读工具只能通过正确模块进入对应工具面，公共工具面始终保持只读。

**Blocked by:** 无。04 的未完成 stdio 写门禁已明确延期；本票保留既有公共只读门禁，不依赖该半成品。

**Status:** completed

- [x] Agent 与公共协议工具索引、查找和模块配置互相独立。
- [x] 未知模块启动即失败，显式空配置产生空工具面。
- [x] 公共工具面即使误配写模块也拒绝列出和调用写工具。
- [x] 既有 Agent 绑定、门禁、审计与 MCP 调用行为保持兼容。
- [x] 原有与新增聚焦测试通过，并形成独立提交。

**Verification:** 聚焦单元、Spring/Testcontainers、stdio 与 HTTP 传输验收套件全部通过；
同时验证新环境变量优先、旧 `MCP_MODULES` 回退、显式空 Agent 清单和公共内部工具不可发现。
