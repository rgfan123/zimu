# 02: 收编京东库存阻断工作台改进

**What to build:** 运营人员在当前收敛架构中获得票 09 的库存阻断卡片和就地处置抽屉，同时保留冻结基线之后的接口契约与界面行为，不重复引入旧工作树版本。

**Blocked by:** 01: 建立隔离收敛来源账本与只读门禁。

**Status:** superseded-by-baseline

- [ ] 来源提交 `70ffc9d6` 的用户可见行为在收敛分支上可用。
- [ ] 与当前接口契约冲突的部分按现行契约适配，而不是覆盖回旧形状。
- [ ] 聚焦前后端测试覆盖卡片展示、抽屉动作和错误状态。
- [ ] 该能力形成独立提交并在来源账本中标记为已收编。

## Resolution

不移植 `70ffc9d6`。该提交明确声明只作留档，并指出冻结基线已经包含更完整的
`1f861b29` 与 `8947f4f8`：它们通过按需读取订单事实避免把 PII 扩散进复核 JSON，
同时覆盖更完整的阻断原因展示。两项提交均已验证为当前收敛分支祖先。
