# Issue 审计：#1–60 范围内的 open issue

审计日期：2026-08-29
仓库：`rgfan123/zimu`　本地：`/Users/jerry/zimu-work/integration`（分支 `jry/integration-20260828`）
范围：#1–60 内 **26 张 open issue**（#2–13、#16–20、#30、#36–46、#48–50、#55、#60 已 closed，不在范围）

审计方式：逐条读验收标准 → 到代码 / git 历史 / 生产库找硬证据。全程只读，未改任何文件、未提交、未部署；生产库只发 SELECT。

**结论：关掉 1 张（#15），4 张建议关但未关，21 张确实还没做。**

---

## 一、已关闭（1 张）

### #15 — wayfinder: 静态礼包 BOM 主数据实现方案（地图）

地图票，交付物是「决策定稿」而非代码，关闭条件是所挂子票全部完成。

- **子票 5/5 全部 CLOSED**（均在 2026-08-19）：#16 新旧礼包源文件合并规则、#17 静态礼包主数据 schema 设计、#18 渠道订单中礼包的形态与识别、#19 静态礼包命中后的履约展开边界、#20 礼包 PMS 上架方案。五张票都有写实的 `Resolution（closed）` 结论段，不是 stub。
- 票正文 5 个 checkbox 全 `[x]`，`Decisions so far` 5 条结论齐全。
- 调研产物齐全，`.scratch/static-bundle-bom/research/`：`01-bundle-source-merge.md`（49 礼包权威合并清单 + `01-merged-components.csv` + 解析脚本）、`02-bundle-schema.md`（完整 DDL 草案）、`03-channel-recognition.md`、`05-pms-upload.md`。#19 是 grilling（HITL 决策）票，按票型不产出 research 文件。
- 定稿提交：`68958e00`（14 files, +68652 行）。
- 后续实现已按本图执行（超出本票范围，仅作佐证）：三表 + `order_lines.bundle_id` 落在 `backend/src/main/resources/db/migration/V39__add_static_bundle_master.sql`（:11 `product_bundles`、:38 `bundle_items`、:61 `bundle_aliases`、:77 `order_lines.bundle_id`），`source_channel_bundles` 落在 `V40__add_wangqi_source_bundle_mappings.sql:32`。

**已在关闭评论中更正的细节**：票里写的「Flyway V36 落地」是定稿时的版本号预测，实际落在 V39/V40（V36 是无关的 agent 迁移）。决策实质未变。

---

## 二、建议关但没敢关（4 张）

### #26 — platform-online-integration: 11 — Phase 2 发货回传（pushShipmentResult）

**犹豫原因：代码侧三平台全部落地且质量很高，一度判定可关；但生产实证把它拉了回来——「自动回传成功」目前不是稳定态，飞象在生产根本没开。**

已做（很扎实）：
- 三家共用同一契约与围栏：`connector/PlatformConnector.java` 的 `pushShipmentResult(SourceShipmentResult, ExternalWritePermit)`，实现在 `connector/jufubao/JufubaoConnector.java:231`、`connector/caishixian/CaishixianConnector.java:281`、`connector/feixiang/FeixiangConnector.java:347`。真实端点：聚福宝 `/order-supplier/v1/logistics/sub-order-send`（`JufubaoHttpShipmentGateway.java:28,189`）、彩食鲜 `/scc/bbc/order/importDeliverExcl`（`CaishixianHttpShipmentGateway.java:131`）、飞象 `/order/ajaxSendOrderProduct`（`FeixiangHttpShipmentGateway.java:35`）。
  - 口径校正：聚福宝 Java 通道走的是单子单 `sub-order-send`，**不是票面写的 `multi-send`**（后者只在脚本通道 `file/SourceReturnPushService.java:40,293`）。
- **SYNCED / SYNC_FAILED 语义完全满足**：`connector/sync/SourceSyncStatus.java` 五态 `PENDING/SYNCING/SYNCED/SYNC_FAILED/RECONCILIATION_REQUIRED`；落库 `V1__baseline.sql:495-496` + `V54__shipment_source_sync.sql:19-29` + `V84__feixiang_online_shipment_push.sql:15-46`；写入带 CAS（`SourceSyncStore.java:145/166/182` 均 `WHERE sync_status='SYNCING'`）。语义严格——HTTP 200 ≠ SYNCED，必须写后平台终态核验（彩食鲜须状态 4 且运单一致 `CaishixianHttpShipmentGateway.java:168-196`；聚福宝须离开 `no_delivery` 列表 `JufubaoHttpShipmentGateway.java:95-97`），结果未知走 `RECONCILIATION_REQUIRED` 不盲目重放。
- 可重试：`SourceSyncRecoveryWorker.java:29-37`（60s）；`SourceSyncStore.java:413-505` 区分「效果前失效可重试」/「效果后失效必须对账」。
- 测试面厚：`SourceShipmentSyncServiceIntegrationTest`（682 行 15 用例）+ 三份 loopback stub 契约测试（含 `FeixiangHttpShipmentGatewayTest.writeModeOffEmitsNoHttpAtAll`）。
- 提交链：`0e99be4f` / `1b34370b` → `2f1461f7` → `f4189a4d`（自动 Worker）→ `3e29729a` → `04b79c4c` → `48d81b35` → `7036f18a` → `bef613cc` → `7e16375c` → `1014ece4`。

拦住关票的生产事实（我亲自查的）：
- 生产确实开了自动回传：`docker inspect zimu-fulfillment-backend-1` 显示 `SOURCE_SYNC_AUTO_ENABLED=true`、`SOURCE_SYNC_AUTO_OPERATOR=zimu-auto-sync`（源码默认是 `false`，见 `application.yml:92`）。
- 确有一次真实成功：`app.shipment_syncs` 有 `JUFUBAO | SYNCED | 2026-08-29 02:05:18.466431+08`。
- **但这是唯一一次**。`app.audit_logs` 里 `zimu-auto-sync` 每 ~60s 一次的 `shipment.source_sync.check`，最近全部返回 **`SOURCE_SYNC_CHECK_BLOCKED`**（19:04:33、19:04:32、19:02:31、19:00:30、18:55:29…连续无一成功）。「自动回传成功」不是稳定态。
- **飞象在生产没开**：`FEIXIANG_SHIPMENT_WRITE_MODE` 不在生产环境变量里 → 回落源码默认 `OFF`（`application.yml:307`）。`FeixiangShipmentWriteMode.java:9-10` 自己写着企微人工上传是「今天飞象唯一在生产验证过的回传通道」；`V84:52` 记录生产实测走通的是 2026-08-28 16:28 的**人工 CSV 上传**。
- `RECONCILIATION_REQUIRED` 无人工对账界面：`grep -rn "source-sync/reconcile" frontend/` 零命中，前端只是只读标签。

**拍板选项**：(a) 认定「接入完成」即达标（代码确实三家齐活、语义严格、测试厚），关票，另开票跟进 `SOURCE_SYNC_CHECK_BLOCKED` 与飞象启用；(b) 先查清 `SOURCE_SYNC_CHECK_BLOCKED` 的阻断原因、把飞象门闩推到 ARMED/ON 验证一次，再关。
**我的倾向是 (b)** —— 票面第一条写的是「聚福宝发货结果自动回传**成功**」，当前生产是「每次 check 都被挡住」，离「成功」还差一步。

### #22 — manual-review-ready-for-human: 03 — JD 出库预览阻断事项的信息与动作

**犹豫原因：票的意图（盲关 → 可处理）已达成且超额交付，但实现走的是另一条动线，票面点名的两个入口字面上不存在。**

已做：
- 中文标签：`frontend/src/constants/reasonLabels.ts:33` `JD_SHIPMENT_OUTBOUND_PREVIEW_BLOCKED: '京东建单预检未通过'`（提交 `285ae592`）
- 阻断明细渲染，两处都有：复核抽屉 `frontend/src/pages/workbench/ReviewCaseDrawer.tsx:549-570`（四列：商品 / 说明 / 阻断码 / 修正目标）+ 投影层 `frontend/src/presentation/publicReady.ts:545-560 reviewBlockerRows()`（提交 `c9d0d88e`）；发货台 `frontend/src/pages/workbench/blockerGrouping.ts`（`PREVIEW_BLOCKED_REASON` 在 :53，`TARGET_LABELS` 含「收货人地址」「履约方地址策略」）
- DISMISS 兜底：`backend/.../order/OrderMapper.java:345` default 分支
- 投影层测试：`frontend/test/blockerGrouping.test.ts`（16 用例）+ `frontend/test/shippingWorkbenchSkeleton.test.ts:127-184`（路由级）

未做：
- 「前往发货记录确认收货地址」跳 `/fulfillment/shipments`：`grep -rn "'/fulfillment/shipments'" frontend/src` 只命中 `navigation.ts:42` / `routes.tsx:76` / `shellRail.ts:34` 三处注册，**没有任何从复核抽屉或发货台指过去的跳转**。取而代之的是更强的就地方案——`frontend/src/pages/workbench/JdBlockerFixDrawer.tsx:399-427` 在抽屉内直接给省/市/区/乡镇/详细地址四级表单 +「确认地址并重跑预检」（提交 `f0027a1e`）。
- 「重跑库存核对」入口在本 reason 下不可达：`OrderMapper.java:325-346` 的 switch 没有该 reason 分支，落 default 只给 `DISMISS`，因此 `ReviewCaseDrawer.tsx:449` 的 `canRerunStock` 恒 false。就地抽屉给的是「重跑**预检**」（`JdBlockerFixDrawer.tsx:152`），不是「重跑**库存核对**」（那是 `JD_STOCK_BLOCKED` 的 `StockBlockerPanel` 路径）。
- 交互层测试缺：`grep -rn "确认地址并重跑预检\|保存并重跑预检" frontend/test` 零命中。

**拍板选项**：(a) 认定就地处置抽屉等价于票面跳转、「重跑预检」等价于「重跑库存核对」，改票后关；(b) 补 `OrderMapper` 的重跑入口 + 地址表单路由级测试后再关。

### #32 — platform-online-integration: 15 — 聚福宝收货人与字典补抓（HITL，09 的 blocker）

**犹豫原因：blocker 职能已解除（生产实证），但 5 条里 2 条无答案、1 条被反向决策取代。**

- 本票存在的唯一理由是「缺收货人 → 09 端到端验收永远过不了」。现在生产库有 `JUFUBAO | SYNCED | 2026-08-29 02:05:18`，聚福宝全链路真实走通过 → blocker 已解除。
- ✓ **收货人字段路径**：`docs/research/jufubao-supplier-export-api.md:126-128`（§5.2）确认 `sub-order-info` 的 `receipt_user_name` / `receipt_phone_number` / `location`；HAR 实据 `jufubao-har-fulfillment-closure-audit-2026-08-24.md:50,64`；代码真取值 `JufubaoHttpShipmentGateway.java:152-155` → `JufubaoOrderTransform.java:94-101`，完整性门禁 `:291-295`。
  - 同仓自相矛盾：`docs/research/jufubao-pull-push-receiver-closure-2026-08-24.md:17,42,51` 仍断言「当前没有已证实的 receiver 读取路径」。两文件在同一提交 `2f1461f7` 里，未对齐。
- ✓ **`sub-order-info` 调用粒度**：文档 `:128`「按子单读取」+ `:133-134` 四步链路；HAR 显示 `?sub_order_id=...` 单 ID 无批量参数；代码 `JufubaoConnector.java:131-142` 拉取循环内逐单调用。
- ⚠ **物流公司字典快照**：**决策被反向取代**。文档 `:146` 明写「每次执行按 `logistics-company/options` **实时确定性映射，禁止硬编码**」；实现 `JufubaoHttpShipmentGateway.java:159-174 carrierOptions()` → `JufubaoConnector.java:598-607`。字面不满足，但属**已记录的设计取舍**，建议修订验收标准而非补做。
- ✗ **订单状态枚举全集**：文档只有 `NO_DELIVERY`（:75,:125）；`:62` 的 `no_delivery/delivered/all` 是 tab 名不是枚举值；§6 缺口表连一行状态枚举条目都没有。取消态 `grep -rn "CANCELLED\|已取消" docs/research/jufubao*.md` → 0。**能返回全集的端点 `/order-public/v1/sub-order-status/options` 已列在 `platform-apis-overview.md:121`，但从未抓取。**
- ✗ **refresh 端点结论**：`:142`「长期运行需补 refresh 逻辑」是 TODO，既不是「已观察到」也不是「未观察到」。决策 D5 仍开（`platform-api-integration-design.md:335,625`）。

**拍板选项**：(a) 认定 blocker 职能已完成、状态枚举转入 #29（它本来就是 #29 的输入），关票；(b) 先补两条文档结论再关。

### #33 — platform-online-integration: 16 — 飞象发货回填接口抓包（HITL，11 的 blocker）

**犹豫原因：票的技术前提被推翻了——飞象回传根本不是「Excel 批量导入」，而是 form ajax 端点。HITL 抓包实质已完成、契约逐字节进了代码，但一份文档都没改，5 个 checkbox 全对不上。**

- 票假设：真人走一次「批量发货导入」（下载发货表 → 上传 Excel），抓 `deliveryImport` 的 HAR。
- 实际落地：**`POST /order/ajaxSendOrderProduct`**，`application/x-www-form-urlencoded`，无 CSRF token，认证靠 `fxqf_sess` cookie。见 `FeixiangHttpShipmentGateway.java:35,86-96`。`FeixiangShipmentRequest.java:78-80` 的 javadoc 明写报文「与 **2026-08-28 HAR 实测逐字对齐**」——**HITL 抓包确实做过了**。
- 逐条 checkbox：
  - ✗「写入 `feixiang-supplier-export-api.md` 新增『发货回传』章节」——该文档标题只有 §1–§7，**无发货回传章节**；更糟的是 §7.6（`:226-230`）仍写「本次只做拉取…`POST /order/ajaxSendOrderProduct` **未实现、未验证**」，与已上线的实现直接矛盾。`git show --stat 3e29729a` → 18 个文件全是 .java，**零 docs**。
  - n/a「上传文件格式与列结构 / 与 `SourceReturnExport` 模板比对」——前提不成立，是逐行 form POST 不是文件上传。
  - ⚠「成功/失败判定」——**代码已实现、文档未记**：`FeixiangHttpShipmentGateway.java:109-141`（空 body → UNKNOWN；`<` 开头 HTML 登录页 → UNKNOWN；`status==1` → ACCEPTED；`status==0` → REJECTED；其余 → UNKNOWN，fail-closed）。
  - ◐「拉取侧区间口径」——**下单 vs 发货口径已结论且有生产实证**：`feixiang-supplier-export-api.md:162-166` 记录旧实现传的 `start_time`/`end_time` **平台根本不认**（真名是 `start_create_time`/`end_create_time`），生产库实证「拉到过的全部 8 行，拉取日与下单日 100% 相同，跨 5 天两渠道零反例」→ 即创建/下单时间。**但 `end_time` 含不含当天仍未解决**：仅 `:73`、`:148` 两条且都指向已废弃的 `deliveryExport`；§7.5 未验证清单里没有 `end_create_time` 含当天这一行。
  - ✗「总览文档 §4 飞象行更新」——`docs/research/platform-apis-overview.md:57` 逐字仍是「未抓包，需补抓（页面有 deliveryImport 导入线索）」，`:19`、`:137` 同样未改，11 天未动。

**拍板选项**：(a) 认定目标已由 `ajaxSendOrderProduct` 路线达成，关票 + 另开一张「文档回写」小票；(b) 就在本票下把三件事做完再关——把 javadoc 里的契约誊进文档新增章节、更新总览 §4、补一句 `end_create_time` 含当天「未观察到」的登记。

> 无论选哪个，`platform-apis-overview.md:57` 与 `feixiang-supplier-export-api.md` §7.6 已经在**主动误报**飞象状态，建议尽快修。

---

## 三、确实还没做（21 张）

### 地图 / 总览票（按规则，子票未全完则不关）

| # | 标题 | 现状 |
|---|---|---|
| 1 | jry: 实施地图 — Agent 平台化（13 票总览） | 13 张子票 #2–#14 中 #14 仍 open，按规则保留；#2–#13 已全部 closed |

### repo-design-hardening 系列（9 张，全系列零进展）

一个横跨所有分支的事实一次性否掉整组：仓库 423 个提交、全部 56 条分支的任何版本里，**从未出现过 ESLint、Prettier、spotless/jacoco、TanStack Query/SWR、openapi-typescript**。

| # | 标题 | 现状 |
|---|---|---|
| 47 | 04 — 静态检查与覆盖率接入 CI | 前端无任何 lint/format 配置与依赖；`backend/pom.xml:141-153` 只有 `spring-boot-maven-plugin`；`.github/workflows/ci.yml` 只跑 backend `mvn test` 与 frontend typecheck/test/build，**无 lint、无覆盖率**。票里说的 8 处 `eslint-disable` 已涨到 **12 处**且全是死注释（`frontend/src/hooks/useAsync.ts:44`、`pages/fulfillment/SalesOutboundPage.tsx:139/179/218` 等） |
| 51 | 08 — 前端类型改由契约生成（expand） | 无生成器接入，`frontend/src` 下无 `generated/` 目录。**别误判**：blocker 票 07 已完成（`16981740` + `backend/src/test/java/cn/zimu/fulfillment/contract/OpenApiContractConsistencyTest.java` + CI 的 OpenAPI contract gate），但那是「后端 spec ↔ 手写 openapi.yaml」比对，与「openapi.yaml → TS 类型」无关。**#51 的 blocker 已解除，可以开工** |
| 52 | 09 — 页面按模块批次切换到生成类型（migrate） | 前置 #51 未做，无生成类型可迁；票里也没有批次清单 |
| 53 | 10 — 删除手写前端类型（contract） | `frontend/src/api/types.ts` 仍在，从票里记的 1229 行涨到 **2007 行**；`agentTypes.ts` 311 行同样手抄。「严格对照 openapi」注释原样健在（`types.ts:2`、`agentTypes.ts:2`、`endpoints.ts:2`） |
| 54 | 11 — 表的写路径归属登记与跨路径写守卫 | 无归属清单文档、无 ArchUnit（`grep -rln "com.tngtech" backend/` 零命中）、无守卫测试。**问题在恶化**：JdbcTemplate 文件数 52 → **131**，原生写 120 → **300** |
| 56 | 13 — 京东履约实现下沉到 connector 层 | 京东实现原封不动住在通用履约包 `cn.zimu.fulfillment.fulfillment`：`ShipmentJdTrackingBackfillService.java`(1052 行)、`ShipmentJdStockCheckService.java`(812)、`ShipmentJdOutboundPreparer.java`(794)、`ShipmentJdOutboundService.java`(710) 等，**JD 相关合计 4785 行**（票里记的是「三千多行」）。无履约方抽象接口；依赖方向**反着**——11 处从履约包 import `connector.jd.*` 具体类（如 `ShipmentJdStockCheckService.java:12`） |
| 57 | 14 — 接入前端数据层并完成单页试点（expand） | `grep -rn "tanstack\|react-query\|useQuery\|swr" frontend/` 零命中；整个前端源码里连 `cache` 这个词都没出现过 |
| 58 | 15 — 页面批次迁移到数据层（migrate） | 前置 #57 未做；119 处调用点全部仍在自研 hook 上 |
| 59 | 16 — 删除自研异步 hook（contract） | `frontend/src/hooks/useAsync.ts`(49 行) 与 `usePagedOrders.ts`(70 行) 都在，**119 处调用、分布在 41 个页面文件**。`react-hooks/exhaustive-deps` 禁用仍有 9 处，根因就在 `useAsync.ts:44-45` 的 `[...deps, tick]` 展开数组 |

### manual-review-ready-for-human 系列（2 张，#22 见上）

| # | 标题 | 现状 |
|---|---|---|
| 21 | 02 — JD_TRACKING 冲突类复核证据专用渲染 | **后端证据早就齐了，前端一个字段都没渲染**：`ShipmentJdTrackingBackfillService.java:441-445` 已把 `erp_delivery_no`/`tracking_candidates`/`jd_status`/`carrier_mapping` 写进 detail，但 `grep -rn "tracking_candidates\|carrier_mapping\|jd_status" frontend/` **零命中**；`publicReady.ts:231-345` 的 `REVIEW_FACT_GROUPS` 没有这三个 reason_code，抽屉落 `REVIEW_FIELD_LABELS` 白名单兜底，只有 `message` 能过。另：「不会修改运单事实」文案矛盾**仍在**——`ReviewCaseDrawer.tsx:102` 这么写，但 `ReviewCaseResolutionService.java:388-399` 对 TERMINAL_EXCEPTION 确实 `UPDATE app.shipment_jd_outbounds SET tracking_query_status='TERMINAL_REVIEWED'`，且该状态是不可逆的自动回填终止闸（`ShipmentJdTrackingBackfillService.java:730-733`）。**这是正在误导操作人的真实缺陷** |
| 23 | 04 — 复核信息补齐 | 4 条只做了 1 条。✓ SKU 类已补（`OrderCreateService.java:918-924` + `skuEvidenceItems():929-960`，提交 `43976793`；前端 `publicReady.ts:585-610` + `ReviewCaseDrawer.tsx:492-522`；测试 `frontend/test/skuMappingReviewEvidence.test.ts`）——但只覆盖 `SKU_MAPPING_REQUIRED`/`SKU_MAPPING_CONFLICT`，票里点名的 `MAPPING_MULTIPLIER` 走 `quantityFactGroups()` 无商品名。✗ SOURCE_FOLLOWUP：后端已写（`ShipmentTrackingService.java:304-307,331-334`），前端白名单没放行。✗ 企微识别失败类：`IntentRouter.java:98-111` 写入的 6 个键在 `REVIEW_FIELD_LABELS` 里**一个都没有**。✗ REJECT：后端 `OrderMapper.java:338-339` 仍下发，前端零实现，**后端也根本没有 reject 端点**——「看得到动不了」原样存在。建议直接选「移除」，删 `OrderMapper.java:339` 的 `"REJECT"` 是一行改动 |

### platform-online-integration 系列（4 张，#26/#32/#33 见上）

| # | 标题 | 现状 |
|---|---|---|
| 24 | mcp-order-fulfillment 04 — MCP 刷新平台订单闭环工具 | **四个工具名在 `backend/src` 零命中**。MCP 注册表 `mcp/McpToolRegistry.java:51-60` 聚合六个 provider 共 43 个工具，无一匹配；`McpProtocolAcceptanceTest.java:175-209` 用 `containsExactlyInAnyOrder` 精确锁定 31 个对外工具名，四个都不在。`PlatformOrderRefreshService` 在 `mcp/` 下零引用。`git log --all -S 'refresh_platform_orders'` 只有 `c9588da3`——那是**写票**的提交（4 个 .scratch 文档，294 行，零行 Java）。注意 `get_import_batch_progress`（`McpDomainReadTools.java:206`）是**另一个**工具，明写不返回姓名电话地址，与本票要的收货人摘要正相反 |
| 25 | 10 — 健康检查与监控 | ✓ AuditLog 齐（`PlatformOrderRefreshService.java:350,492-503`；`ScheduledPlatformPullService.java:204-216`；`SourceImportService.java:240,416,1084-1097`）。◐ testConnection 有真 HTTP 但**只探登录**，票面点名的只读动作（彩食鲜 orderList / 聚福宝 orders/query / 飞象统计）一个都没调，且缓存会话可零网络返回 OK（`FeixiangPullClient.java:182-183`）——`FeixiangPullClient.java:22-31` 自述 2026-08-26 丢单事故中 login-only 探测全程返回 OK，「准确诊断」被同仓事故证伪。✗ **告警分级灰级在 DB 层被物理禁止**：`V1__baseline.sql:722` `CHECK (severity IN ('YELLOW','RED'))`，`OperationalAlertSeverity.java` 只有两值；且拉取失败**根本不建 operational_alert**。✗ 失效演练零记录 |
| 27 | 12 — 横切治理 | ◐ 凭据轮换文档在（`docs/research/platform-credential-rotation.md:3` 2026-08-18 定稿，90 天周期、责任人是岗位不是人名），但**从未执行过一次**，且**文档红线已被代码违反**——文档 `:30`「凭据只走环境变量、不入库」，而 `connector/credential/DbConnectorCredentialsResolver.java:38-50` 是 DB 优先、env 兜底（AES-GCM 密文入 `connector_configs.config`）。✗ golden 样本**三份在库但零代码引用**（`grep -rn "research/golden" .` 5 条全在 markdown），且飞象那份是空壳且已过期（`"headers_21": []`、`"endpoint": "deliveryExport"`——该端点已于 `3d3ad282` 删除）。✗ 失效演练未做。◐ `.env.example:76-99` 三平台配置齐、`.gitignore` 齐，但 D1/D2 作废只记在 `.scratch/`，`platform-api-integration-design.md:160,165,580,620,621` 仍列为未决 |
| 28 | 13 — 调度与拉取节奏 | 主提交 `ac2b1a95`（2026-08-28 21:15，7 文件 766 行）。✓ **单实例不重入做得很扎实（三层）**：单线程执行器（`ScheduledPlatformPullTrigger.java:44-49`）+ `V83__scheduled_pull_runs.sql` 的 `run_key UNIQUE` 配 `ON CONFLICT DO NOTHING RETURNING`（`ScheduledPullRunStore.java:58-70`）+ `PlatformPullSingleFlight` 的 `pg_try_advisory_lock`。✓ 每日 ≤2 次可核验（`application.yml:361-368` 两个 cron 9:00/18:00 + `run_key={date}:{slot}` 唯一约束）。✓ 不产生空批次。✓ 双通道互斥已消失（`scripts/run_platform_pull.sh` 不存在）。✗ **无错峰表**（三渠道在 `PlatformOrderRefreshService` 内串行，`grep -rni "错峰\|stagger"` 零命中）。✗ **无重试 2 次指数退避**（拉取路径零命中）。✗ 3 天连续闭环**时间上不可能**——特性上线不足 24 小时。**另外生产实证很糟**：`app.connector_configs.last_pull_at` **8 个渠道全为 NULL**（该列零写入点），FEIXIANG 的 `last_error` 是 `{"status":"FAILED","message":"飞象平台自报窗口内有 9 单，但列表页解析出 0 单…"}` |
| 29 | 14 — 消失检测语义与规范修订 | **0/5，一行实现都没有**。`docs/excel-closed-loop-spec.md:292-303`（§11）10 个原因码里**无 `SOURCE_STATUS_CHANGE`**；该词只作为提案出现在 `docs/research/platform-api-integration-design.md:361,626`，**backend/frontend 零命中**。`grep -rniE "disappearance\|待发货列表消失\|差分\|防抖"` 后端零命中；`PlatformConnector.java:29-36` 的 `pullOrderChanges`/`pullCancellations` 仍是 default 空实现，javadoc 明写「按合并裁决由『消失检测』（ticket 14）替代」——替代物本身没建。`CONTEXT.md` 零命中。**其 blocker 票 02 已落地**（`SourceImportService.java:296 importStructured`），所以不是被阻塞，是纯未动工 |

### 其余（4 张）

| # | 标题 | 现状 |
|---|---|---|
| 14 | jry: 13 — meta agent endpoint | 实现存在但**走的不是本票的验收契约**：本票要 `POST /api/meta-agent/run` 的 202 异步闭环，实际落地的是 `agent-console 06`（`ab5cf228`）的**同步** `POST /api/v1/agents/meta/conversations`（`agent/meta/MetaAgentController.java:19,28-35`，返回 200；该包内 `grep "202\|ACCEPTED"` 零命中）。`MetaAgentOutcome.java:26-35` 不含门禁结果、不含建议用例 PENDING 记录。两入口形态不一致——人工建草稿是 202 + `run_mode=PREVIEW`，meta 入口 `AgentRuntimeFacade.java:270-282` 给 runMode 传 null，而 `AgentObservability.java:24-25` 明写「null 按 LIVE」→ **meta 运行污染 LIVE 统计**。端到端在生产是坏的（open issue #198 记录「Meta-Agent 建 agent 每次必 500」，因生产 `MCP_MODULES` 不含 `control,write`）。✓ 唯一达成的是 NEEDS_INPUT + 冲突 slug 拒绝（`MetaAgentConversationService.java:99-102`、`AgentDraftService.java:86-88`、测试 `MetaAgentDraftIntegrationTest.java:238`） |
| 31 | mvp-productization: 09 — 操作人身份与管理凭据发布门禁 | 5 条主验收的源码证据都在，**但票里 4 条运行态门闩逐条实测仍成立**：① 生产库 `pg_roles` 只有 **`fulfillment` 一个非内置角色且是 superuser**，四角色拆分一个都不存在；② `pg_database` 显示 `fulfillment_hub`/`metabase`/`postgres` 全归同一 superuser；③ `grep -rln "CREATE ROLE\|GRANT " backend/.../db/migration/` 零命中（目录已到 V84），`docs/postgres-role-migration.md` 自陈不是已迁移的证据；④ 生产 `docker ps` 显示 nginx 仍 `0.0.0.0:80->80`，**无 443、无 HTTPS 终止**。`README.md:38` 自己也仍写着共用一个 PostgreSQL login。票正文「本票必须保持 in-progress」的判断至今准确 |
| 34 | wecom-long-connection: 07 — 真实验收与切换门禁 | 代码侧很完整（`connector/wecom/WecomLongConnectionClient.java` 1251 行含 30s ping / 指数退避 / `disconnected_event`→KICKED，提交 `9152d014`），**但本票只要「真人在真实企微跑一遍并留记录」，这件事零证据**：`.scratch/wecom-long-connection/` 下无 acceptance 目录、无 report、无日志，本地票文件 17 行零 checkbox；五项人工验收无一有结果。「旧 Webhook 链路确认失效」**未验证且部分被反证**——Java 侧 `WecomCallbackController` 已删，但免认证路由仍在：`common/web/ProductionRequestAuthenticationPolicy.java:45 new RouteRule("/wecom/callbacks/", Requirement.NONE)` 与 `docker/nginx/default.conf:86` |
| 35 | wecom-message-intake: 13 — 一期整链验收与真实企微门禁 | 7 条里 6 条已勾且有证据（`.scratch/wecom-message-intake/acceptance/report.md`）。唯一未勾的第 6 条实测「三分之二完成」：单聊文字 ✓ 2026-08-17（`channel_messages id=1`）、群聊 @机器人文字订单 ✓ 2026-08-17（草稿 `OD-3-3`、复核事项 `RC-WECOM-30b756f37393413f`），**图文/图片 ✗ + 真实数据确认订单/运单的 HTTP 证据 ✗**——报告自陈「图文/图片证据验收仍待用户动作」。`git log -- .../report.md` 只有 `9152d014` 一条，无更晚记录；两个验收脚本补不上（`mcp-runtime-acceptance.py:31` 写死 `WECOM_ENABLED=false`，`browser-acceptance.py` 全程 mock） |

---

## 四、值得单独拎出来的横向发现

1. **契约知识住在 javadoc 而不是 `docs/research/`**。`3e29729a` 是最清楚的例子：完整抓到的飞象写契约，**零文档文件改动**。三份规划文档（`platform-apis-overview.md`、`platform-api-integration-design.md`、`platform-api-integration-plan.md`）全部冻结在 2026-08-18，现在**主动误报**两个 connector 的真实状态。代码内部也自相矛盾——`FeixiangPullClient.java:42-44` 仍声明 `ajaxSendOrderProduct`「一律未实现」，而 `FeixiangHttpShipmentGateway` 已经实现了它。

2. **「代码存在但未接通」四项**（区别于验收达成，建议单独立票）：
   - `connector_configs.last_error` —— 写入路径完整，**零读出口**，`ConnectorConfigView` 无该字段，API/UI 都看不到
   - `connector_configs.last_pull_at` —— 建表了，**零写入点**，生产 8 渠道全 NULL
   - `docs/research/golden/*.json` —— 三份在库，**零代码引用**；飞象那份还对着已删端点
   - 飞象在线回传 —— 代码完整 + 测试完整，生产门闩 `OFF`，企微人工上传仍是唯一实际通道

3. **#21 优先级建议提高**：后端 detail 字段早就齐了，纯前端一个事实组 + 一段测试就能收口；而抽屉里那句「不会修改运单事实」与后端不可逆写库行为矛盾，是个正在误导操作人的真实缺陷。

4. **#47 是 repo-design-hardening 里性价比最高的**：装 ESLint + 一个 CI 步骤，装完立刻暴露 #59 要修的 9 个 exhaustive-deps 风险点，等于给后面的票免费体检。

5. **#54 / #56 在恶化**：原生写 120 → 300 处，京东实现在通用履约包里从「三千多行」涨到 4785 行。拖越久成本越高。

6. **两个生产运维问题，可能比票本身更紧急**：
   - `app.connector_configs.last_pull_at` 8 渠道全 NULL + 飞象拉取 `FAILED`（自报 9 单、解析 0 单）→ 定时拉取从未成功落过一次
   - `zimu-auto-sync` 每 60s 一次 check 全部返回 `SOURCE_SYNC_CHECK_BLOCKED`，自动回传实际处于全阻断状态
