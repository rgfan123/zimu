# Issue 审计报告：编号 61–120（open 部分）

- 审计时间：2026-08-29
- 仓库：`rgfan123/zimu`
- 代码基线：`/Users/jerry/zimu-work/integration`，分支 `jry/integration-20260828`，HEAD `1014ece4`
- 范围内 open issue：33 张（61 62 63 65 66 67 68 69 72 75 76 77 78 79 80 91 92 93 94 102 103 104 105 106 107 108 109 110 111 112 118 119 120）
- 生产库核对：只读 SELECT，未发任何写语句
- 代码：只读，未改动、未提交
- GitHub 写操作：仅 6 次 `gh issue close --comment`，无其他

---

## 一、已关闭（6 张）

### #104 zimu-workbench 01 — 工作台导航与岗位视图

- 提交 `68a283f8`（应用外壳换新——244px 侧栏岗位视图、删顶栏、我的工作台板块）、`fdd3a5bb`、`ecec58f4`、`db0928ec`
- `frontend/src/workbenchRole.ts` —— localStorage 键 `zimu.workbench-role`，四个 `responsible_team` + 财务落地别名，`workbenchRoleLabel` 对未知团队原样返回
- `frontend/src/components/layout/WorkbenchRoleSwitcher.tsx:76` —— 首次进入显示「请选择岗位」，不预设默认值
- `frontend/src/navigation.ts:19-29` 一级板块「我的工作台」+ 四入口；`frontend/src/routes.tsx:65-71` 路由注册
- `frontend/src/navigation.ts:117` 演示页 `hideInMenu: true`
- `NAVIGATION_GROUP_SUFFIX` 全 `frontend/` grep 零命中；删除记录 `docs/agents/navigation-admission.md:112`
- `docs/agents/navigation-admission.md:86-90` 实时计数已重算（一级板块 10→11、我的工作台可见叶子 4、作业中心 6→5）
- 本次实跑：`navigation` / `businessObjectNavigation` / `identityBoundary` / `appShellRoute` 等 8 个文件 48 条全过

### #105 zimu-workbench 02 — 调度台直接消费后端 severity

- 提交 `c0098a93` fix(frontend): trust dashboard attention severity (#105)
- `grep -rn CRITICAL_REASONS frontend/src` 零命中（全仓仅 `frontend/test/dashboardDispatchRoute.test.ts:237` 注释提及，作为不许回退的说明）
- `frontend/src/pages/dashboard/DashboardPage.tsx:244` 直接用 `item.severity` 决定颜色
- `frontend/test/dashboardDispatchRoute.test.ts:237`「后端 YELLOW 必须映射 waiting，而不是 CRITICAL_REASONS 的红」
- 本次实跑全过

### #106 zimu-workbench 03 — 复核收件箱按岗位进入并原位处理

- 提交 `8bbf8a2f`（#106 复核收件箱岗位默认团队预筛）、`95862f66`（批量与连续作业）
- `frontend/src/pages/workbench/ManualReviewPage.tsx:37/101-108` 岗位默认团队预筛并写回 URL
- 同文件 `:106` URL 有 `responsible_team` 时以 URL 为准
- 同文件 `:429/:475`「看全部」；`:64/179/184` + `ReviewCaseDrawer.tsx:432/448` 动作只由 `allowed_actions` 决定
- `frontend/test/manualReviewQueueRoute.test.ts:460 / :477 / :497` 三条 #106 专项测试；本次实跑 19 条全过

**关闭时已留档的口径变更**：票面「reason_code 分组折叠队列」未按原形态实现。收件箱最终形态是「筛选下拉 + 批量勾选 + 处理完直接载入下一条」（`95862f66`），reason_code 分组保留在发货台复核预览区（`frontend/test/shippingWorkbenchSkeleton.test.ts:90`）。已在关闭评论里写明，若仍需要分组折叠请另开票。

### #107 zimu-workbench 04 — 发货台启动订单同步并诚实呈现渠道结果

- 提交 `b06a33e3` feat(frontend): add shipping sync workbench (#107)，后续 `8eaeb9ce`、`06ce50e6` 迭代
- `frontend/src/routes.tsx:69` + `navigation.ts:22` 路由与菜单
- `ShippingWorkbenchPage.tsx:194-201` 同步按钮调 refresh、同步中禁用；`:124-125` ref 级重入门禁
- `shippingPresentation.ts:15-66` 稳定码查表，不从自由文本推断；`:94-96/:218` 聚福宝「仅报告未入库」一等状态
- `ShippingSyncResults.tsx:125`「没有新订单」，且受 `shippingPresentation.ts:126-130` 约束（有 failed/skipped 时禁用该文案）
- 不伪造配额：`frontend/test/shippingWorkbenchRoute.test.ts:89` 专门断言
- 本次实跑 29 条全过

### #111 zimu-workbench 08 — 对账台查询并解释七种内外事实状态

- 提交 `ade3d9af`、`bb06d5c9`、`8b601582`
- `frontend/src/routes.tsx:71` + `navigation.ts:29`
- `OutboundReconPage.tsx:164/325-326/337` 三种 query_type/query_value 进 URL；`ReconWorkbenchPage.tsx:12` 复用，零复制
- `OutboundReconPage.tsx:129` JD_UNAVAILABLE→「京东侧未取到」与 JD_NOT_FOUND→「京东无记录」分开；`:148-149` 其余四态各自行样式
- `ReconWorkbenchPage.tsx:131/143` `¥ ——`、`:175-176` 金额未纳入本期、`:67-71` 明写不编数
- `frontend/test/reconWorkbenchRoute.test.ts:70/83/134/156/178/196/256`；本次实跑 12 条全过

### #72 review-actionable-detail 02 — 其余复核事项家族按同一模式补齐明细

- 提交 `082a0dce` feat(review): surface actionable facts for review families (#72)
- `frontend/src/presentation/publicReady.ts:262-287`（客户映射）、`:288-305`（承运商映射）、`:306-307`（数量换算两码共用）、`:308-322`（导入数据）、`:323-342`（导出后改单）
- 白名单沿用 01：`:233-236` 复用 `SKU_MAPPING_FACT_GROUP`；`:192/194-229` 逐字段 PII 说明
- 可复用结构：`reviewFactGroups()` `:346-348` + `factGroupRows()` `:351-357`；抽屉统一入口 `ReviewCaseDrawer.tsx:141/502/525-526/751`
- 「整行消失无提示」已消除：`:7` `SOURCE_NOT_PROVIDED` + `:354-355` 兜底
- 测试：`reviewActionableDetail.test.ts:68/95/115/142/161/184/271/281` + `reviewFactGroups.test.ts` 12 条；本次实跑全过

---

## 二、建议关但没敢关（4 张，需人拍板）

### #108 zimu-workbench 05 — 发货台展示履约链路并直达卡点

**做到的**：提交 `814d8fe9`（七指标/八段链路/复核分组/告警区一次全部在位）、`6bfb044e`（页头一行化）。七指标 `ShippingWorkbenchPage.tsx:151-166`、八段链路 `:168-190`、无聚合 API 的段落诚实占位「暂无汇总」`:189`（京东出库提交段）、聚福宝降级显式标注、复核区按 reason_code 分组且跳转与计数同接口同筛选（`frontend/test/shippingWorkbenchSkeleton.test.ts:90`）。

**卡在哪**：票面 Scope 的「其他阶段跳既有批次、发货、运单、来源回填页并附筛选参数」与 Acceptance 的「全部真实跳转」**没有实现**——八段链路的 `zs-pstep` 是纯静态 div，无 `onClick`、无键盘处理（`ShippingWorkbenchPage.tsx:255-278`）；只有七指标卡有页内锚点跳转（`#zs-pipe` / `#zs-review` / `#zs-fail`）。

**为什么需要拍板**：后续「发货台一页闭环 1/n–5/n」系列提交（`67d93698` `e419d652` `5c45b0c8` `b0707e7a` `f0027a1e`）把设计从「跳出去到列表页」改成了「在本页就地处置」——`AwaitingTrackingSection`（运单）、`PendingSourceSyncSection`（来源回填）、`PendingConfirmationSection`（待确认）都是这个方向的产物。也就是说票面要求的「跳走」很可能已被「不用跳」取代。请确认这是有意的取代还是遗漏：如果是取代，改票面后即可关；如果不是，需要补段落级跳转。

### #109 zimu-workbench 06 — 发货台完成整批确认与京东失败重试

**做到的**：`PendingConfirmationSection.tsx:39/81` 已实现「调用前 confirmed_at 已存在则显示『已确认过』」（票面第 4 条）；确认入口在工作台已就位（提交 `01a57f77`「工作台加确认发货入口，闸门与后端同源」）；复核未清零时提供真实收件箱入口（`ShippingWorkbenchPage.tsx` 的 `#zs-review` 区块）。

**卡在哪 / 为什么需要拍板**：

1. 票面第 1 条「need_review=0 且 rejected=0 才允许确认」**已被后端语义取代**——提交 `d212ad94`「来源批次支持部分确认，跳过阻断行先发能发的」把闸门改成 `batch.confirmable`（`PendingConfirmationSection.tsx:66-70`），允许带阻断行做部分确认。这不是没做，是需求变了。
2. 票面第 3 条「调用既有 jd-outbound-submit 重试失败项」在工作台**不存在**；重试按钮只在文件作业页 `SalesOutboundPage.tsx:471`「重试京东建单」。
3. 票面第 2 条「就地显示京东 SDK 路由与导出结果」在工作台也没有——工作台确认成功只提示「本批次已确认，履约文件已生成」（`PendingConfirmationSection.tsx:24`），京东 SDK 路由计数在 `SalesOutboundPage.tsx:296`。

`git log --grep '#109'` 零命中，这张票没有专属提交。建议按现状重写票面（部分确认语义 + 决定重试入口放哪），而不是照原文施工。

### #110 zimu-workbench 07 — 采购台从 Agent 建议进入人工采购

**做到的**：提交 `8927d463`、`e06f7900`。页面 `/workbench/procurement` 已注册（`routes.tsx:70` + `navigation.ts:28`）；建议卡展示为什么/建议/provider·model·prompt_version 留痕/requires_human（`procurementSuggestion.ts:115/120/139`）；`excluded_candidates` 与剔除理由并排展示（`procurementSuggestion.ts:87-93`、`ProcurementSuggestionCard.tsx:2-3`）；Agent 稳定错误码走结果路径不抛异常（`procurementSuggestion.ts:144`）；业务动作只跳 `/procurement/tickets`（`ProcurementSuggestionCard.tsx:107-108`，ADR 0010 注释在旁）；待处理工单数与空态（`ProcurementWorkbenchPage.tsx:121/216-219`）。测试 `procurementSuggestion.test.ts` 5 条 + `procurementWorkbenchRoute.test.ts` 3 条全过。

**卡在哪**（两条 Scope/Acceptance 未满足）：

1. **「最久未动的那条等了多久」没有实现**。四个指标是「待我确认建议 / 待处理工单 / 部分到货 / 今日新增工单」（`ProcurementWorkbenchPage.tsx:112-123`），没有 oldest-untouched。工单表只逐行显示 `created_at`（`:246`）。
2. **INVALID_PARAMETERS 没有落到字段**。`useSuggestionRunner` 用 `Promise.allSettled`，rejected 一律计入 `failed` 并汇总成「本次没有产生建议（N 张工单运行失败）」（`:78-97`、`:183`），拿不到「具体哪个字段不合法」（票面故事 40）。

判断：主体已交付，差这两条。要么补这两条再关，要么确认它们可以不做后直接关。

### #80 agent-console 06 — 对话式创建 Agent（后置）

**做到的**：提交 `b5e63618`。五条验收在代码上都能对上——`/agents/new` 路由（`routes.tsx:89`）、左对话流右实时草稿预览（`AgentCreatePage.tsx` Row/Col + `:121` 草稿预览 Card）、三种 outcome 各有独立呈现（`:29-39` outcomeTag、`:159-179` SUCCESS、`:181-194` NEEDS_INPUT、`:196` REJECTED）、草稿落库 draft 未启用（后端 `AgentDraftService.java:62/92`，UI `:163` 明写「尚未启用」）、建议评测用例落 PENDING（`AgentDraftService.java:218-228`）、页面内零启用路径（`:4-6/21` RED_LINE 常量 + `endpoints.ts:1101-1105`「接口上不存在任何启用路径」）。后端端点存在：`backend/src/main/java/cn/zimu/fulfillment/agent/meta/MetaAgentController.java:17/27`。

**卡在哪 —— 顺带查出一个真 bug**：

多轮对话的会话线程很可能是断的。前端发的是 `body: { message, threadId }`（`frontend/src/api/endpoints.ts:1112`），`apiRequest` 直接 `JSON.stringify(body)` 且**没有 camel→snake 转换**（`frontend/src/api/client.ts:199`）；后端 Jackson 全局 `PropertyNamingStrategies.SNAKE_CASE`（`backend/src/main/java/cn/zimu/fulfillment/common/web/JacksonConfig.java:14`），`MetaAgentMessage` 期望 `thread_id`（`MetaAgentController.java:41`）。也就是说 `threadId` 每轮都被绑成 null——NEEDS_INPUT 之后用户补充信息，模型拿不到上文。

另外该页无 route 级测试（`agentConsoleRoute.test.ts:311` 只断言「新建 Agent 通往对话式创建页」），且票面 Status 是 `deferred（用户裁定后置；P1–P5 先行）`，而 P3（#77）前端整块未开始。

判断：先修 threadId 再关，或确认单轮可用即可接受后关。

---

## 三、确实还没做

### 前端设计加固族 #61–#69

`git log --grep 'repo-design-hardening'` 与 `--grep '#61'…'#69'` **全部零命中**——这批票在本分支上没有任何专属提交。现存的「沾边」成果都能追溯到 #36 / #95 / #96 / #97 / #104 / #144。

| 票 | 现状一句话 |
|---|---|
| **#61** URL 层级对齐菜单层级 | 部分：只有 `NAVIGATION_GROUP_SUFFIX` 被 #104 顺带删了；路径仍与分组错位（`navigation.ts:104-110` 京东六子页是 `/fulfillment/jd-*` 却挂在 `/system` 分组），无旧→新重定向（全仓 `<Navigate` 仅 3 处），反查打分函数 `navigation.ts:122-135` `routeMatchScore` 仍在并被 `:164` 与 `routes.tsx:165` 消费 |
| **#62** 详情改为可分享的真实路由 | 未开始：六类对象零详情路由，抽屉全由本地 `useState` 驱动（`FulfillmentTasksPage.tsx:54`、`ShipmentsPage.tsx:257`、`ProcurementTicketsPage.tsx:38`、`ChannelMessagesPage.tsx:37`、`ManualReviewPage.tsx:116`、`AlertsQueuePage.tsx:33`） |
| **#63** 列表筛选与分页进 URL | 部分（存量）：筛选只在 #95/#96/#97 做过的几个页面；**分页全站无一进 URL**（`queuePagination.ts:18-19` 及另 9 处 `useState` 分页）；`FilterBar.tsx:8` 注释明写「不规定筛选状态如何存放」 |
| **#65** 响应式外壳与令牌化 | 未开始，代码里有明写的 defer：`frontend/src/components/layout/shell.css:358-366`「响应式外壳归 #65，本轮不做适配」，窄屏是 `display:none` 整块隐藏且无唤出入口；尺寸/字号/圆角仍是 88 处 px 字面量，`saasTheme.ts` 只有色板无 spacing/radius 令牌 |
| **#66** 面包屑与隐藏页归属 | 未开始：`grep -rn "Breadcrumb\|面包屑" frontend/src frontend/test` **零命中**；底座 `navigation.ts:154-205` `navigationTrail` 现成但无 UI 消费；隐藏页高亮判定是精确相等 `AppLayout.tsx:167` |
| **#67** 危险动作统一确认组件 | 未开始：无统一组件；31 处逐案 `Popconfirm`/`Modal.confirm` 散在 9 个文件；无防绕开检查（唯一结构性门禁 `antdBoundary.test.ts` 管的是 antd 布局组件边界，与危险动作无关） |
| **#68** 表单校验与字段级错误 | 未开始：`frontend/src/api/client.ts:99-140` 长条件链原样健在；`field_errors` 只在 `types.ts:56-60/68` 有类型定义、全 `frontend/src` **零消费**（后端确实在产出，`InternalOrderApiTest.java:905` 等六处断言），全被 `client.ts:110` 一句「提交内容有误」吞掉；`rules={[` 36 处 vs `<Form.Item` 99 处 |
| **#69** 加载态统一与可访问性 | 部分（存量）：`PageState.tsx`（#36）、`DataTable.tsx`（#97）、两个 overlay 焦点契约（#104 `db0928ec`）已有；缺三选一适用规则的成文、缺 14 个 `<Drawer` + 17 个 `<Modal>` 的焦点陷阱、**缺焦点管理测试**（`grep -rln focus frontend/test` 只命中 `saasTheme.test.ts` 的配色令牌断言） |

### Agent 中心族 #75–#79

后端读写端点齐备（`AgentReadController.java`、`AgentDefinitionWriteController.java` 五个 202 异步写动作），生产库 `app.agent_definitions / agent_eval_cases / agent_eval_results / agent_runs / agent_tool_calls` 五张表存在。缺口全在前端。

| 票 | 现状一句话 |
|---|---|
| **#75** Agent 列表页 | 部分：列表主体齐（`AgentsListPage.tsx:79-200`），但 **分页完全不存在**（`:313` `pagination={false}`，`agentPresentation.ts:344-350` 只写 state/draft/write），运行状态是三值不是四种组合（`agentPresentation.ts:37-49`，后端 `AgentReadService.java:173-178` 把 enabled 两种情况折叠成 `NO_ACTIVE_VERSION`），模型「未公开 vs 未配置」三态未落到本页（`AgentDetailPage.tsx:98-102` 用 `?? '—'`） |
| **#76** Agent 详情 | 部分（只差一个参数名）：当前生效字段与版本链全齐（`AgentDetailPage.tsx:61-214`），守卫豁免空数组给「默认守卫全部生效」`:159`，最近 5 次运行 `:216-255`；但「查看全部」跳的是 `/agents/runs?slug=`（`:222`）而验收写的是 `?agent=` |
| **#77** 草稿确认闭环与启停 | **未开始（前端）**：`endpoints.ts:1060` 注释「只读，写动作等 T11」，`agentsApi` 只有 4 个 GET；`set-enabled`/`drafts`/`rollback` 在 `frontend/src` 零命中；`AgentDetailPage.tsx:7`「写动作本期一律不做」。后端五个端点已就绪，是纯前端接线工作 |
| **#78** 运行记录页与运行详情 | 部分：列表/详情/LIVE-PREVIEW 隔离/错误码/输入摘要说明都在；缺两条——`?agent=<slug>` 不生效（`agentPresentation.ts:175` 只读 `slug`），无「跳同 run_id 审计日志」入口（`frontend/src/pages/agents/` grep `audit-logs` 零命中） |
| **#79** 评测用例页与人工纠正回流 | 部分：按版本分组 + INVARIANT/QUALITY 分两组已做（`EvalsTab.tsx:143-188`）；缺 `case_key`（全仓零命中）、行内不展示 `metric_kind`、**「从复核结果生成 PENDING 评测用例」的入口整条链路不存在**（`ReviewCaseDrawer.tsx` 无该按钮），「新增用例」按钮本身不存在（只有 `:158-163` 一条 Alert 说明去处） |

### 万齐 API 线 #91–#94

**代码层面完全不存在**。全仓 `wanqi/WANQI/万齐` 命中 100% 落在 52 列 Excel 导入链路。

| 票 | 现状一句话 |
|---|---|
| **#91** 短信登录与会话门禁 | 未开始：`connector/` 下无 wanqi 包（4 个 `PlatformConnector` 实现是 wecom/jufubao/feixiang/caishixian）；`grep sendSms\|smsCode\|sms_login` 在 backend 与 scripts 零命中 |
| **#92** API 拉取待发货订单 | 未开始：万齐进系统的唯一路径是 `SourceFileParser.java:53/282/481` 的 52 列 Excel（`SourceChannel.java:15-16` 枚举注释自述「导出 52 列来源文件」） |
| **#93** 京东运单回填万齐 | 未开始，且是**显式留空**：`V42__add_wanqi_52_source_channel.sql:34-36`「来源回填契约尚未确认，不配置承运商映射」，V43–V84 无一条补上；回填路径被硬闸门挡死 `file/TrackingFileService.java:735-737`（`if ("WANQI".equals(...)) return null;`）；`deliveryCompany`/`deliveryList` 全仓零命中 |
| **#94** 真实 API 全链路验收 | 前置全缺，无从验收；`docs/acceptance.md:47-52` 的「未解除的外部 gate」列表里万齐**连条目都还没进** |

### #102 jufubao-shipment-p0 04 — 真实账号与测试订单外部验收

**外部验收未做**，票面 blocked-external 成立，不得用现有证据结案。

- 代码前置（#99/#100/#101，均已 closed）确已落地：`connector/jufubao/JufubaoHttpShipmentGateway.java:23-28` 五条平台路由；写后回查 `:91` `pollOrder(subOrderId, state -> "NO_DELIVERY".equals(state.status()))`；可写状态门禁 `connector/sync/SourceSyncPolicy.java:91-93`；幂等 store `JufubaoShipmentAttemptStoreJdbc.java`（V54）；契约登记 `docs/openapi.yaml:1470/1533/1564/1587`；10 个后端测试。
- **真实平台验收证据为零**，且已有文档明确否认：`docs/research/jufubao-har-fulfillment-closure-audit-2026-08-24.md:122`「『当前 Java 已真实账号验收』——不成立。HAR 证明的是一方浏览器 happy path，不是 Java 客户端的线上执行结果」；同文件 `:113-115`、`:129` 把 #102 要求的授权真实订单验收列为未闭合缺口。
- `docs/acceptance.md:47-52` 仍把聚福宝真实 API 凭据列在未解除 gate 里。

### 采购比价线 #118 / #120（含范围外前置 #121 #122）

**生产库核对（只读）**：`information_schema.tables` 中 procurement 相关只有 baseline 四张（`procurement_tickets` / `procurement_ticket_items` / `procurement_receipts` / `procurement_receipt_items`），**无报盘表、无建议表、无盯盘品表**；`procurement_tickets` 的列里**没有**供应商与实际成交价；`procurement_receipts` 的列里**没有**凭证附件引用。与 #120 票面自述的「2026-08-23 库内核实」基线完全一致，说明这条线自那天起零推进。

| 票 | 现状一句话 |
|---|---|
| **#121**（范围外前置） | 未开始：`grep "ALTER TABLE app.procurement" db/migration/*.sql` 零命中；`ProcurementReceiptInput.java:10-16` 五个字段无 attachment（D6 凭证必传校验因此也没有） |
| **#122**（范围外前置） | 未开始：`scripts/` 下 10 个文件无牧集/肉交所脚本；`牧集\|肉交所` 全仓只命中 4 个文档（ADR 0007/0008/0009 与 DEMO-BRIEF），**零代码命中**；`PlatformScriptRunner` 模式存在但没挂任何采集脚本 |
| **#118** 建议引擎与每日调度 | 只完成了前置：`common/config/ScheduledTaskPoolConfiguration.java:11-29`（`@EnableScheduling` + `taskScheduler` bean，pool-size 13）由提交 `1b34370b` 落地。主体全缺——无纯规则建议引擎（现存 `ProcurementPriceAgent` 是走大模型的，剔除规则是「离群/缺失/映射失效」，不是「剔最高最低各 1 个」）、无 08:00 cron（全仓 `@Scheduled(cron=)` 只有 `MessageRetentionCleanupService.java:56` 的 03:30 清理与 `ScheduledPlatformPullTrigger.java:51` 的平台拉单）、无手动触发端点、无采集/建议的 agent_run 留痕 |

### #119 zimu-workbench-backend 01 — 发货台聚合计数端点

**未开始**。后端 60+ 个 Controller 里不存在任何 `/api/v1/workbench/**` 路径；`docs/openapi.yaml` 无 workbench 路径（唯一命中是 `:2184` 的 tag 描述文字）。前端仍在 ADR 0006 第一阶段：`frontend/src/pages/workbench/useShippingSummary.ts:1-6` 文件头自述「既有列表接口 size=1 只取 total_elements 拼真数……#119 聚合端点落地后只改本文件」，`:88-91` 拼 `processing_stage=X&page=0&size=1`，`:100-135` 并发 8 次 `countOf()`。回填失败计数尚无数据来源（`ShippingSummaryCounts` 接口里根本没有该字段；`source_return_exports.push_status='FAILED'` 只在 `file/SourceReturnPushService.java:68/81` 的服务内部条件里出现，无全局清单端点）。

### #112 zimu-workbench 09 — 对账结果下钻到完整证据链

未做。`git log --grep '#112'` 零命中。对账结果目前只有「订单」一层可下钻（`OutboundReconPage.tsx:209/262` 的 `/orders/:id?return_to=`），订单行 / Shipment / 运单 / 来源导入 / 审计日志五层无入口；且它的前置 #61 / #62 / #63（对象详情路由与 URL 契约）本身也没做。

---

## 四、两张 spec / 总览票的处置

按规则「只有子票全部做完才关」，两张都**保留 open**：

- **#103 zimu-workbench spec** —— 子票 #104–#112 中，#108 有未完成的段落跳转、#109 未做（且门禁语义被取代）、#112 未做，因此不关。本轮关掉了 #104 #105 #106 #107 #111（5/9）。
- **#120 procurement-price spec** —— 三张子票 #121（数据层）、#122（爬虫）、#118（建议引擎与调度）全部未完成；已落地的只有决策文档本身（ADR 0007–0010，提交 `e86d55aa`）。不关。

---

## 五、一句话总览

33 张里关了 6 张（#72 #104 #105 #106 #107 #111），4 张卡在人拍板（#108 #109 #110 #80），其余 23 张确实还没做或只做了前置。最集中的两块空白是**前端设计加固族 #61–#69**（八张票在本分支零专属提交）和**万齐 API 线 #91–#94**（代码层面完全不存在，现有「万齐」全是 52 列 Excel 导入）。顺带查出一个真 bug：Meta-Agent 对话页的 `threadId` 因前端不做 camel→snake 转换而每轮被绑成 null，多轮对话很可能是断的。
