# Issue 审计：#121–#170（open）

审计时间：2026-08-29
判据基线：`/Users/jerry/zimu-work/integration`，分支 `jry/integration-20260828` @ `1014ece4`
上游仓：`/Users/jerry/Documents/kehuzx`，分支 `codex/zimu-kehuzx-integration` @ `f09f5f2`

范围内 open issue 共 37 张。**已关闭 4 张，其余 33 张保持开启。**

---

## 判据说明（三条，影响所有结论）

1. **主干是 `master` 一系，不是 `main`。** `origin/main` 是另一条无关快照历史（`b65ec8cd`）。真实工作在 `jry/integration-20260828`（相对 origin/main 领先 386、落后 5）。PR #152 的 base 就是 `master`。

2. **生产 == 当前分支。** zimupc 上跑的镜像标签是 `real-1014ece4`，恰好等于本分支 HEAD；flyway 停在 V84（`feixiang online shipment push`，2026-08-29 02:03 应用成功）。因此「生产库里没有这张表」= 「迁移不存在」= 「没做」，这条推论在本次审计中成立。

3. **历史里做过 ≠ 现在还在。** 本仓有系统性的「陈旧工作树 `git add -A` 误删」事故（详见下方拍板事项 A）。所有结论一律以**当前工作树**为准，并对分支做过 `git merge-base --is-ancestor` 验证。

---

## 一、已关闭（4 张）

### #129 agent-observability: token 计量修准 + 聚合视图（多轮工具少算）
核心提交 `5633abf1`，已验证为 HEAD 祖先（来源分支 `codex/agent-observability-129` 尖端 `e50bb3e9` 同样已并入）。
- 每轮累加：`LangChain4jRuntimeAdapter.java:101` 累加器提到循环外，`:119` 每次 chat 返回即 `tally.plus(...)`，工具轮 continue 前先记账。
- 失败也记：`:177` 改为 `finally { recordTokens(binding, tally); }`，四条出口共用一次写入，覆盖写因此变成幂等。
- 聚合端点：`GET /api/v1/agent-runs/token-usage`（`AgentRunReadController.java:56`），分组用枚举 `TokenUsageGroupBy`（AGENT/DAY/BUSINESS_ENTITY_TYPE），契约登记在 `docs/openapi.yaml:44,3374`。
- 测试：`LangChain4jRuntimeAdapterTokenAccountingTest.java` 六个用例（:71/:90/:107/:120/:131/:144）+ `AgentTokenUsageSummaryIntegrationTest.java`。
- **一处有意偏离已写进关闭评论**：超阈值不落 `operational_alerts` 行（该表有 `CHECK (num_nonnulls(order_id, order_line_id, fulfillment_id, shipment_id) > 0)`，Agent 跑飞没有业务主体），改为写进 `token_usage` JSONB 的 `over_threshold` 并由聚合端点的 `over_threshold_runs` 暴露，理由记在 `JdbcAgentObservability.java:23-29`。按意图口径判完成。

### #146 agent-wecom-workflow: foundation 02 — Business Follow-up 材料建档与 +1 发起整理
`V65__business_followups.sql` 头注即写 `(Issue #146)`；实现提交 `10e8a60d` / 候选 `f77dcce` 均为 HEAD 祖先；V65 于 2026-08-27 20:43 在生产应用成功。八条验收逐条有据：稳定业务号 `BF-%010d`、`ON CONFLICT (message_submission_id)` 幂等、外键钉死 `agent_definitions(agent_slug, version)`、存证事务内无 HTTP 客户端、taskKey 唯一 + 租约心跳、`stage` 与 `processing_status` 两列分离、PII sanitizer + 两个针对性测试。
**票上那条「pushed/PR-open only, not merged」的评论已过期** —— PR #152 已于 2026-08-27 合并。

### #130 agent-wecom-workflow: Kehuzx 01 — 用只读事实生成可核对客户草稿
`V66` 头注 `Issue #130: fail-closed Kehuzx read evidence...`；候选 `ac4d6ec` 为 HEAD 祖先。七条验收逐条有据：`KEHUZX_MCP_ENABLED` 默认 false、五工具白名单 + 本地先拒 + 远端 tools/list 多广播即 fail closed、`zimu_source_summary`/`kehuzx_source_summary` 两列分离 + output_schema 强制标 `source ∈ {ZIMU, KEHUZX}`、六个稳定 failure_code + 零/多命中入 ReviewCase、`agent_run_id` 双向可追溯、`KehuzxMcpReadClientTest` 11 个用例含 token 不泄漏两条。
**同样，票上「尚未合并/部署」的评论已过期。**

### #147 agent-wecom-workflow: foundation 03 — Draft 的 +1 卡片审批与版本门禁
候选 `da514a0` 为 HEAD 祖先；V67 于 2026-08-27 20:54 在生产应用成功。七条验收有据：`UNIQUE (followup_id, version)` + 五态草稿机、群聊 `maskName/maskPhone/maskAddress` 确定性脱敏且按钮只在服务端 `confirmable` 为真时渲染、`designated_reviewer_operator_id`/`decided_by_operator_id` 双外键 + `GATEWAY_ASSERTION` 认证、Approval 三重唯一 + `source_event_id` 部分唯一索引、回调线程纯 DB 三步、触发器 `guard_wecom_card_event_first_facts` 锁死首事实、20 个集成测试。
网关逐人身份确实落地：`docker/nginx/entrypoint.sh:44-65` 用 `$remote_user` 覆写 X-Operator 并附 X-Gateway-Assertion（token ≥32 字符强校验），后端消费在 `RequestContextFilter.java:128,138`。
**两处如实备案在关闭评论里**：(a) 「5 秒内已受理」只有架构保证（回调无长事务/无网络调用），没有对 5 秒的显式计时断言；(b) 卡上零参数按钮只有「确认」一个，重做/补充/暂停走深链回 REST（因为都强制填理由），四种决定都落 Approval，符合条款字面但不符合「卡上四个按钮」的直觉。

---

## 二、建议关但没敢关（3 张）

### #131 Kehuzx 02 — 已确认跟进幂等建档或关联客户
**为什么犹豫**：Zimu 侧七条验收我核到的都满足了 —— 唯一写路径 `BusinessFollowUpCustomerAssignmentExecutor.java:122-153` 的 `create_customer`，`:134-147` 已有持久化 request 时**先 reconcile 再说**、reconcile 不可用时落 `RECONCILIATION_REQUIRED` 而非「已知失败」，`requestId` 由唯一 Assignment 派生，14 个集成测试覆盖拒绝/已有客户/多候选/版本过期/并发/不确定。最后一条「契约未齐时生产写入保持禁用」也满足（`KEHUZX_MCP_WRITE_ENABLED` 与 `FOLLOWUP_ASSIGNMENT_WORKER_ENABLED` 双门闩默认 false）。
**卡在**：这张票的目的是「已确认跟进真的在 Kehuzx 建出客户」，而它依赖的 #145 上游契约票还开着（kehuzx PR #1 仍是 OPEN、side branch 未合 main），生产六张表全 0 行、zimupc 上根本没有 kehuzx 容器，这条写路径**一次都没跑通过**。按「代码验收」该关，按「业务目的」不该关。**请拍板取哪个口径。**

### #148 foundation 04 — confirmed version 生成可追踪 Assignment
**为什么犹豫**：投影机制本身是完整的 —— `V68` 头注写 `Issue #148`，三重外键锁死「只有当前 CONFIRMED + 有效 Approval 可投影」，`UNIQUE (followup_id, draft_version, task_type, logical_target)` + `ON CONFLICT DO NOTHING` + 回读首次行，状态机含 `RECONCILIATION_REQUIRED`，17 个集成测试含 `partialUnknownOutcomeDoesNotRollbackSuccessfulSiblingInSameConfirmedVersion`。
**卡在**：票面要求「客户建档、**样品、正式订单、Excel 或人工任务**都有明确执行者」，但 `task_type` 被 DB CHECK 锁死为只有两个值（`V68:13` + `V70:9-11`：`KEHUZX_CUSTOMER_LINK` / `KEHUZX_CUSTOMER_CREATE`），样品/正式订单/Excel/人工任务**物理上写不进这张表**；投影代码 `AssignmentApplication.java:76` 也只有二元分支。另外 `due_at`/`priority` 是硬编码（`:93-95` 固定 +1 day / NORMAL），`openapi.yaml:4767` 的 enum 更窄（连 `KEHUZX_CUSTOMER_CREATE` 都没补）。骨架完成、任务族只落了一种。

### #151 foundation 00 — Kehuzx 前端构建与 E2E 基线恢复
**为什么犹豫**：活确实干了，在 `/Users/jerry/Documents/kehuzx` 的单提交 `28a2ab7`（18 文件 +239/−36）：双 webServer（`start-e2e-backend.mjs` 105 行 + vite）、`frontend/tests/app.spec.ts` 恰好 7 个 `test(`、`frontend/src/` 下 `@ts-ignore`/`as any` 命中 0、README 写齐 Python 3.11–3.13 与 Node 20.19+/22.12+ 及 PowerShell 路径。
**卡在两点**：
1. **有一条验收明确不达标。** 票面要求「页面结构、视觉语言和业务交互保持现状，仅做构建与测试所需的最小修复」，但 `28a2ab7` 在 `OrdersPage.tsx` 新增了 `pasteCopiedTemplate`（:32）+「粘贴模板」按钮（:90）约 50 行**新业务交互**——补的是一个此前从未实现、但测试已存在的功能。这不是最小修复。
2. **未合并，且没有 CI 佐证。** kehuzx `main` 只有 4 个提交（`d7b5761`），`codex/zimu-kehuzx-integration` 未合入（已验证 NOT MERGED）；该仓无 `.github/workflows/`，「lint/build/e2e 通过」只有 `dist/` 与 `.last-run.json` 的旁证，没有实跑证据。

---

## 三、确实还没做（30 张）

### procurement-price 线（5 张）—— 整条线零实现
| # | 现状 |
|---|---|
| #121 价格数据层与盯盘品 API | 报盘/建议/盯盘品三张表一张都不存在，无任何迁移创建；`ProcurementController` 仍只有 list/detail/retry/cancel-remaining 四个端点。只有 ADR 0007/0008 落地 |
| #122 牧集/肉交所爬虫移植 | `scripts/` 下无 muji/roujiaosuo 脚本，全仓「肉交所/牧集」只命中文档与未合并的 `.scratch` 票面原文；`PlatformScriptRunner` 通道在，但没有任何价格采集脚本接入 |
| #123 工单商业化两字段 + 回执凭证必传 | `procurement_tickets` / `procurement_receipts` 自 `V1__baseline.sql` 后再无任何 ALTER，无供应商、无实际成交价、无凭证附件列；`ProcurementReceiptInput.java:10-16` 无附件字段，故「缺凭证即拒绝」不存在 |
| #124 盯盘品清单管理界面 | 全前端「盯盘」0 命中，procurement 路由仅 3 条，无隐藏路由无清单页 |
| #125 工单页参考价/成交价/凭证 UI | `ProcurementTicketsPage.tsx` 中供应商/成交价/凭证/参考价/upload 全部 0 命中；回执区是纯只读时间线，连确认到货按钮都没有 |

> 提醒两个同名陷阱：`V45__procurement_price_excluded_candidates.sql` 是 procurement-price-**outliers** 线的 agent 定义播种（只动 `agent_definitions`），不建任何价格表；前端 `ProcurementSuggestionCard` 是 **#110** 的比价 Agent 建议卡，不是 #121 的每日报盘建议。
> 佐证：生产 `agent_runs` 里 `procurement-price-agent` **一次都没跑过**（只有 intent-recognition 16 / meta-agent 7 / customer-followup-agent 1 / data-query-agent 1 / fulfillment-file-agent 1）。

### agent-wecom-workflow 剩余（6 张）
| # | 现状 |
|---|---|
| #127（spec） | 12 张子票中仍有 #131/#132/#133/#134/#145/#148/#149/#150/#151 未完成，按规矩 spec 不关 |
| #145 foundation 01 — Kehuzx MCP 身份/幂等/对账契约 | Zimu 客户端侧完成度很高（写门闩强校验、23 工具面精确断言、HS256 短时 grant、三态结局 + 只读回读）；但主体是上游活，7 个提交（`4f74454`…`ecd48b1`）+ 42 个协议测试全在 kehuzx side branch 上，**PR #1 仍 OPEN、未合 main**。另 Zimu 侧无对应 MCP 契约文档（验收第 8 条） |
| #132 Kehuzx 03 — 样品需求进客户订单中心 | 只做了「意图冻结」：`V71` 加 `execution_plan` JSONB + `BusinessFollowUpExecutionPlan.java` 字段白名单校验。**`execution_plan` 全仓只被写入读出、从未被执行**；`Executor.java:42-50` 只 dispatch 两种客户任务，第三种直接抛异常。上游 `create_sample_request` 已就绪（`mcp_server.py:1013`），缺口纯在 Zimu 侧 |
| #133 Kehuzx 04 — 正式订单及商务条件 | 同上。`order_type` 严格等于 `formal` 的校验有了（`ExecutionPlan.java:77-80`），但没人调 `create_order_draft`/`confirm_order`/`update_commercial_terms`；这些工具名在 `KehuzxWriteGateway.java:33-44` 列了却无任何调用点 |
| #149 delivery 01 — Excel 生成 + 企微回送 | followup 包 38 文件检索 excel/workbook/poi/xssf/xlsx **0 命中**，检索 export/media_id/upload **0 命中**；`V70` 的 task_type CHECK 从物理上排除交付类任务；Assignment 状态机无 QUEUED/SENDING/ACK。底层能力（`ContentAddressedFileStore`、`WecomMediaUploader` init/chunk/finish）齐备但没被调用。git 全历史无 #149 提交 |
| #150 deploy 01 — 双仓私网 Compose 发布与回滚门禁 | 代码与手册层其实相当完整：Zimu 侧 77 行接线（外部网络 `zimu-kehuzx-private`、MCP 不 publish、`/kehuzx/` 走 Nginx），kehuzx 侧 `compose.production.yml` 八 service + postgres digest 钉死 + `SEED_DEMO:false` + 独立命名卷 + 全文件无 `ports:` + `deploy/README.md` 的备份/可恢复性检查/部署后验证/回滚步骤。**但「执行记录」层为零**：无发布登记、无验证记录、无回滚演练，`docs/ops/deploy-runbook.md` 全文 0 处 kehuzx，`.github/` 0 处 kehuzx，且上游未合并。另 `.env.example` 漏登记实际在用的 `KEHUZX_ENABLED` 与 `ZIMU_KEHUZX_NETWORK` |
| #134 Kehuzx 05 — 真实环境验收首条闭环 | 前置票未解除；双门闩默认关；**生产硬证据**：`business_followups`/`draft_versions`/`approvals`/`assignments`/`kehuzx_read_evidence`/`kehuzx_read_failures` 六表**全 0 行**，`internal_operators` **0 行**（没人能被授权成 +1），zimupc 上无 kehuzx 容器/网络/卷，170 张已发企微卡里无 followup 卡域 |

### design-deepening 线（7 张）—— 主因是一次误删，不是没做
| # | 现状 |
|---|---|
| #153 恢复京东商品名比对内核（p0） | `JdGoodsNameMatch.java` 不在树，`NO_REFERENCE`/`MISMATCHED`/`NAME_REFERENCE_MISSING` 全库 0 命中；两个消费方各留一份重复 `normalize()`+`token()`（`JdSkuMappingCheckService.java:260,264` 与 `ShipmentJdSkuMappingGateService.java:699,703`）。**本票问的「是有意回退还是误回退」现已有答案：误回退，且是第二次**（见拍板事项 A） |
| #154 JdIscGateway 传输内核与 PII 投影归一 | `JdIscGateway` 0 命中。七个 client 各复制一份 `SUCCESS_CODES`（:37/:45/:47/:31/:32/:43/:52）、一份 `normalize()`、一份凭据装配。`connector/jd/` 主源码行数：deepening 完成态 3655 → HEAD 4684，与 deepening 之前完全相同，即已达成的 −1029 行被 100% 吐回。票上 2026-08-26 的评论自述「未 push、未 merge、未 deploy」，`ffa67f9` 经验证非 HEAD 祖先 |
| #155 JdSubmissionState 政策模块与 Plan 类型化投影 | **PARTIAL**。`requiresReconciliation` 确实只有一个实现（`JdShipmentSubmissionPlan.java:68`）；但政策数据寄居在 `ShipmentJdOutboundPreparer.java:45-51`，企微失败卡片 `JdOutboundFailureCardSource.java:54,83` 直接手写 SQL `sync_status='SYNC_FAILED'` 未消费政策，`retryable` 政策不存在。Plan 类型化投影完全未做：`request()` 仍是裸 Map，`ShipmentJdOutboundService.java:669` 的 `@SuppressWarnings("unchecked")` 强转仍在，`ShipmentJdOutboundPreview.java:21-23` 仍按键名硬编码只遮 `pin` 一个字段 |
| #156 LeasedTaskLoop 收拢八个异步 worker | `LeasedTaskLoop` 0 命中，无任何通用骨架。手写 claim 循环的 worker 现在是 **15 个**（比开票时更多），且三种循环形态并存；`AsyncTaskStore` 公开方法 **17 个**（目标 ≤6）；claim/lease 在 store 层还另外重复了 5 份 |
| #157 六项顺手收拢挂账 | **按设计保持开启** —— 票面自述「This is a debt ledger, not a single-session implementation ticket」，触碰到对应区域时才完成所属项。抽查：第 5 项（internal 镜像共享委托）已达成（`InternalAgentRunReadController` 与对外面共用 `AgentRunReadService`，随 #129 落地）；第 2 项（MasterData 拆分）部分达成（已分出 Bundle/ProductArchiveSheet/SkuArchiveExport，但 `MasterDataController`/`MasterDataService` 仍在）。其余四项未逐条核实 |
| #158 平台回传工作台 UI | 8 条验收 1 MET / 1 PARTIAL / 6 UNMET。**注意别误判**：`1014ece4`/`7e16375c`/`48d81b35` 不是本票实现——它们做的是逐单面板文案层 + 工作台加一段扁平待办清单，而本票要的是按平台聚合、可批量、故障隔离。后端 `SourceShipmentSyncController.java:52` 的 `/source-sync/batch-execute` 已存在但**前端零调用**；抽屉无 `useEffect`（`SourceSyncPanel.tsx:32-33` 注释明写「检查不在抽屉打开时自动跑」，是刻意设计）；中汇被排除出清单却没给替代入口。**含一处线上 PII 泄漏，见拍板事项 B** |
| #159 ShipmentStatus 枚举 + 生命周期判定模块 | 后端**无任何名为 ShipmentStatus 的 Java 类型**，14 个 grep 命中全是 docs/前端 TS 类型/wayfinder 票。四个收编点位全部退回手写字面量（`SourceSyncFactsReader.java:64`、`OutboundReconService.java:973` 同一规则的第二份复制、`ShipmentJdTrackingBackfillService.java:751`、`ShipmentJdOutboundPreparer.java:53`）。棘轮测试 `ShipmentStatusTest`/`ShipmentStatusRatchetTest` 也不在树中。票面自述的 `codex/design-deepening-02-verify`（`cbcdb529`）经验证非 HEAD 祖先 |

### inventory-v1 线（11 张）—— 一行都没做，确认
`InvenTree` 在**全部分支的全部历史**里出现 **0 次**（`git log --all -S 'InvenTree'` 计数为 0，大小写不敏感亦为 0；从未有过任何名含 inventree 的文件）。无 Adapter、无 SKU-Part 映射、无导入批次/导入行、无 OCR 草稿、无 StockItem/Attachment、无盘点、无 FEFO、无 external snapshot 表；生产无相关表、无 InvenTree 容器。

`ready-for-agent` 标签只表示「可以开工」，与完成度无关——这批全部是可以开工但尚未开工。

| # | 现状 |
|---|---|
| #160（spec） | 十张子票全未开工，spec 不关 |
| #161 InvenTree 1.5.2 部署与能力验收基线 | 零引用，无容器、无 compose 条目、无版本锁定记录 |
| #162 Adapter/Auth 与 SKU-Part 映射 | 无 Adapter 类、无映射表 |
| #163 表格 + 原图上传与 OCR 库存草稿 | 无导入批次/行/OCR 证据表 |
| #164 人工确认并幂等写入 StockItem | 无写入路径、无 EVIDENCE_PENDING 状态 |
| #165 盘点导入、差异确认与库存流水 | 无盘点批次、无 Tracking 映射 |
| #166 临期/过期 Dashboard 与提醒 | 无效期模型、无阈值配置、无提醒 |
| #167 订单 Allocation 与 FEFO 预占/释放 | 无 Sales Order 镜像、无 Allocation 映射 |
| #168 京东/供应商 External Location 快照边界 | 现有 `provider_stock_snapshots` 与 `InventoryOverviewService`（只读观测总览，NOT_OBSERVED 不补零）是**既有功能**，正是本票要求「继续工作」的那部分，不构成本票实现 |
| #169 受控端到端验收 | 前置全未做 |
| #170 用今天的产品 + 原料表格/图片做真实验收 | 前置全未做；且标签是 `needs-info`，还卡在等 Jerry 提供今天的库存表格、原图清单、行↔图关系、目标仓库/库位、原料 SKU 裁决 |

---

## 四、需要拍板的事（4 条）

### A. 【最重要】一次没人发现的误删事故，#153/#154/#159 只需要 revert 一次 revert
提交 `f4189a4d`（"feat(source-sync): 来源平台自动回传 Worker（默认关闭）"，**是 HEAD 祖先**）在提交说明只字未提的情况下，一次性删掉了 #153/#154/#155/#159 的全部实现：`JdIscGateway.java`(212行) / `JdPiiProjection.java` / `JdAuditProjection.java` / `JdWarehouseAuditProjection.java` / `ShipmentStatus.java`(74行) + 两个棘轮测试 / `JdGoodsNameMatch.java`(49行) + 测试 / `docs/adr/0012-knowledge-ownership-deepening.md` / `.scratch/design-deepening-20260825/` 下七张设计票原文，同时七个 JD client 全部回胀。

**误删证据（决定性）**：七个 client 在 `f4189a4d` 之后的 blob SHA 与 deepening 之前的 `f0027a1e` **逐字节相同**（`JdWarehouseClient` 两处均为 `fe1ce88b`，`JdOrderClient` 均为 `9a0e3e65`），且这些 blob 至今仍在 HEAD 上——典型的「陈旧工作树 `git add -A`」签名。

**而且这事已经发生过一次并被诊断过。** `f4189a4d` 的父提交 `1135bae5` 的说明原文写着：「…提交说明对此只字未提，判定为并行会话用陈旧工作树 git add -A 造成的误删，非有意回滚。」`1135bae5` 恢复了第一次误删，紧接着的 `f4189a4d` 又原样删了一遍——这次没人发现。旁证：ADR 编号 `0012` 已被 `69b418eb`（2026-08-26，晚于误删）重新占用为 `0012-uiux08-naming-alignment.md`。

**好消息：全部 21 个被删文件在 `1135bae5`（HEAD 祖先）上完好**，可 `git checkout 1135bae5 -- <paths>` 取回，不需要重写。恢复时要同时把七个 client 从 `f0027a1e` 那版切回网关版，并处理 ADR 0012 编号冲突。

**建议**：优先恢复 #159 的棘轮测试（守门文件清单 + 字面量预算）——那是唯一能自动拦住第三次的机制。

### B. 【线上风险，与排期无关】#158 的收货人姓名泄漏在工作台首屏
`ShippingWorkbenchPage.tsx:584` 直接把 `row.receiverName` 拼进列表行，链路是 `pendingSourceSync.ts:20` 声明 → `:65` 从 `receiver_name` 取值。反讽的是详情抽屉反而没显示 PII（`SourceSyncPanel.tsx:95-120` 只有承运商/运单号/地址状态标签），正好和票面要求（列表不暴露、详情才展示）反过来。#158 其余七条只是「没做」，唯独这条是已经在线上的泄漏。删掉拼接并从 `pendingSourceSync.ts:20/65` 去掉该字段即可，`shipmentNo + trackingNumber` 已足够定位单据。

### C. kehuzx 的活全在未合并的 side branch 上，「算不算做完」需要定口径
kehuzx `main` 只有 4 个初始提交（`d7b5761`），全部集成工作（#145 七个提交、#150 `fbf3c98`、#151 `28a2ab7`、#132 `f09f5f2`）都只活在 `codex/zimu-kehuzx-integration`（`f09f5f2`），已验证 NOT MERGED，PR rgfan123/kehuzx#1 仍 OPEN。该仓还**没有 CI**（无 `.github/workflows/`），所以「测试通过」全部只有本地旁证。
另外 `#131` / `#133` / `#134` 在 kehuzx 的**所有 ref 里都没有任何提交提及**。
我按「未进主线 = 未交付」处理，全部保持开启。**若你认可「写完即完成」的口径，#145/#151 可以关**（但 #151 还有第二节说的验收违反问题）。

### D. 生产上已经跑着一个独立的原料库存系统，和 #160 的 InvenTree 方案关系待定
zimupc 上有容器 `yuanliaokc-mcp`（镜像 `yuanliaokc-mcp:real-2e05d24`，已 Up 35 小时且 healthy），独立网络 `yuanliaokc_default`、独立卷 `yuanliaokc-data`，`DATABASE_URL=sqlite:////data/scd_wms.db`、`UPLOAD_DIR=/data/uploads`。
#160 的问题陈述是「子牧当前以 Excel 和图片人工维护产品 + 原料库存」——但生产上显然已经有一套在跑的原料 WMS。这批 11 张票是 2026-08-27 开的，而 `docs/research/yuanliaokc-deploy-mcp-plan-2026-08-27.md` 是同一天的另一个方案，方向还相反（明确不起 Postgres / 不跑 Web）。
**在开工 #161 之前，建议先确认 InvenTree 方案与 yuanliaokc 是替代、并存还是已被取代**，否则 11 张票可能建在过时前提上。我没有深入读那套系统（读取被权限拦了），只报告存在性。

---

## 附：本次审计用到的可复现证据命令

```bash
# 主干与生产对齐
git rev-parse --short HEAD                     # 1014ece4
ssh zimupc "docker ps --format \"{{.Names}} {{.Image}}\""   # real-1014ece4
ssh zimupc "docker exec zimu-fulfillment-postgres-1 psql -U fulfillment -d fulfillment_hub -t -A -c \"select version,description from flyway_schema_history order by installed_rank desc\""   # V84

# 判断某提交是否真在主干上（本仓的核心陷阱）
git merge-base --is-ancestor <sha> HEAD && echo MERGED || echo NOT-MERGED

# inventory-v1 零实现
git log --all --oneline -S 'InvenTree' | wc -l  # 0

# 生产库只读（Windows cmd：单引号会被吃掉，用 $a$...$a$ 代替字符串字面量，别在 ssh 内用 | head）
ssh zimupc "docker exec zimu-fulfillment-postgres-1 psql -U fulfillment -d fulfillment_hub -t -A -c \"select tablename from pg_tables order by 1\""
```
