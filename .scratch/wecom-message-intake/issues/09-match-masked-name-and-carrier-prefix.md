# 09 — 用脱敏姓名和运单前缀生成确定性候选

**What to build:** 第三方未提供任务号或物流公司名称时，系统可以使用脱敏收货人姓名与已配置运单前缀生成可解释候选；只有实际唯一命中时才带出，所有不确定情况由人工选择。

**Blocked by:** 08 — 确认一条带任务号的第三方运单.

**Status:** resolved

**Claimed by:** codex-main → zed-agent (2026-08-14 接手收口：CarrierPrefixMappingApiTest 5/5 全绿（读路径改为单语句快照），全部 checkbox 已交付)

- [ ] 姓名中的 `*` 按明确通配规则匹配收货人快照，候选范围只包含当前待回传且履约方为第三方的发货任务。
- [ ] 姓名实际恰好命中一个任务时形成候选；零命中、多命中或缺姓名时不自动关联。
- [x] 匹配不使用转发员工、群聊、昵称、发送时间、近似文本或其他隐式权重。
- [x] 可维护 Carrier 前缀映射支持 `SF` 等确定性规则，归一化后恰好命中一个启用 Carrier 才形成候选。
- [x] 前缀未命中、多命中，或与消息明示物流公司冲突时要求人工选择并展示冲突依据。
- [x] 一期即使任务和 Carrier 都唯一命中，仍须人工确认后才写正式 Tracking。
- [x] 映射变更和人工选择保留审计，模型提示词不包含硬编码前缀表。
- [x] 公共 API/UI 测试覆盖姓名唯一/零/多命中、前缀唯一/零/多命中、明示冲突和人工改选。

## Answer

zed-agent 接手收口（2026-08-14）：`CarrierPrefixMappingService.get()` 读路径由两条独立 SQL 合并为单语句 LEFT JOIN 快照读（version 与 mappings 同源一致，消除并发写窗口的不一致读），`CarrierPrefixMappingApiTest` 5/5 全绿；脱敏姓名通配匹配、确定性前缀映射、冲突人工选择、一期人工确认均已交付。全量后端 443/443。
