# 牛羊礼包精准成本核算与礼包整理 Agent map

Type: map
Status: charting complete — 13 张票已建，前沿 6 张（01/02/03/04/05/13）

## Destination

一个**可运行**的礼包成本核算能力：给定礼包组件清单与收货区域（或客户），系统返回**逐项可审计**的落地成本拆解与毛利，并通过 **MCP 只读工具面**供礼包整理 Agent 调用；在正向核算可信之后，再交付反向组合（给定客户采购价、重量、库存与临期约束，搜出候选组合）。

> 执行带进地图：设计票产出决策，构建票产出可运行代码，不止于文档。
> 判定「可信」的标准：用户给的样例礼包（牛羊礼包 3800g / 8 组件 / 客户采购价 300 / 对外售价 400）逐项成本能与人工核算对齐，且换区域、换装箱方案时数字按预期变化。

## Notes

**领域**：B 端生鲜食材订单履约与仓储物流（先读 `CONTEXT.md`；`docs/agents/domain.md`）。技术栈 Java 21 / Spring Boot 3.5 / PostgreSQL / MCP。
**固定输入**（均在 `input/`，图片无电子原件，已逐项转录）：
- `input/A产品成本核算26.3.29.xlsx` —— 成本源表（从微信消息归档抢救回来，见票 11）
- `input/jd-b2c-cold-chain-price-table.md` —— 京东 B2C 冷链仓储价格表（入库/存储/订单生产/增值/耗材全档）
- `input/jd-express-freight-zones.md` —— 京东快递 5 分区运费表 + 规则缺口清单

**跟踪器**：本地 Markdown，见 `docs/agents/issue-tracker.md`（map + `issues/NN-slug.md`，`Type:`/`Status:`/`Blocked by:` 行；resolve 时追加 `## Answer` 并回写 map 的 Decisions so far）。
**技能**：`grilling` + `domain-modeling`（任何 HITL 票）、`research`（外部知识票，AFK）、`tdd`（构建票）。
**HITL 原则**（沿用 `wayfinder/map.md`）：数值精度、索引、时区等常规工程默认由 Agent 直接按稳健方案裁决并记录；只把会改变业务行为、人工流程、交付范围或不可逆取舍的问题交给用户。
**红线**：
- **业务 Agent 写工具零调用不变式**（见 `.scratch/meta-agent-platform` 票 08）——礼包 Agent 只能拿只读工具，方案落库由应用层做，不由 Agent 写。
- **运费与成本不允许模型推断**——地址→省份、分区、费率、计费重量全部走确定性主数据与规则；模型只负责组合与解释，不负责算钱。
- **费率主数据版本化**——涨价换新版本，历史核算按当时版本可重算。

**已核实事实（2026-08-25，本次建图勘察）**：
- 商品档案现状：`app.products`（V30 后）仅 `purchase_price` / `retail_price` / `other_cost` 三个成本数；`app.skus` 有 `purchase_price` / `retail_price`。装不下成本构成。
- **全库无任何重量/体积字段**（`schema-export-current.sql` 中 `weight`/`volume` 零命中）。`wayfinder/tickets/product-bundle-and-pack-mapping.md` 的「不增加商品净重模型」是把重量排除在 **Demo effort** 之外，非永久否决；本 effort 正当重启。
- **无效期模型**：`app.provider_stock_snapshots` 只有 `stock_num` / `usable_num`，无批次无生产日期；成本表也无保质期列。京东 `StockResult` 带 `batchInfos`（`docs/research/jd-isc-api.md:75`），是否含效期待调研（票 03）。
- **无结构化省份**：`app.orders.receiver_address` 是自由文本 TEXT；结构化 province/city 只存在于京东出库的临时 `ShipmentJdReceiverAddressCommand`，未落库。
- MCP 已存在：`McpToolRegistry` + `McpServer`（stdio 只读面），已有 `list_products` / `get_sku` / `get_inventory_overview` / `get_inventory_detail` / `list_categories` / `list_provider_skus`。本 effort 是**加工具**，不是新建 server。
- 数据源单一：`jdbc:postgresql://localhost:5432/fulfillment_hub`，无第二个库。
- 样例礼包 8 个组件在 `A产品成本核算26.3.29.xlsx` 的 `成品` sheet 全部命中，净重合计正好 3800g；`AI 线下供货成本/份` 合计 ≈ 213.00，`AJ 售价` 合计 = 273。
- **抛重可能改运费档位**：6 号泡沫箱 435×300×250 = 32.6L，按抛重系数 6000 折 5.44kg > 净重 3.8kg。系数与进位规则待向京东确认（票 04）。

## 建图期已定决策（grilling 结论，不再重开）

1. **终点 = 可运行能力**，执行带进地图（非 spec 交接包）。
2. **成本基数口径 = `加工后成本/份`**（只到加工，不含耗材与物流）；`AI 线下供货成本/份` 仅作单品线下供货的对照值。用户原话「AJ 是成本」已核实为口径笔误：`成品` sheet 的 AJ 列是**售价**，AI 列才是线下供货成本。
3. **数据真源 = 本系统 `fulfillment_hub` PostgreSQL**。`A产品成本核算26.3.29.xlsx` 只做**一次性种子导入**，导完不再参与；此后成本维护走商品档案 API/页面。**不建外部 ERP 连接器**。
4. **商品范围 = 全部三类**：自有成品（113 行）、送仓经销品（~109 行）、一件代发经销品（~72 行）统一进 `products` / `skus`，用供货模式区分。一件代发那张表自带产地/发货仓/快递公司/服务区域/发货时效，是运费与时效核算的输入，不丢。
5. **重量模型 = 只在 `skus` 加净重**（`net_weight_g`）；包材自重、箱内外径与体积、冷媒重量归**包材主数据**，礼包毛重与体积在运行时按装箱方案算。换箱子不该改商品档案。
6. **保质期 = 两层**：一期落「商品保质期天数」主数据 + 入库日期估算；京东批次真值（`batchInfos`）由票 03 调研后择机替换，真值优先。
7. **运费 = 京东快递**，5 分区表（1kg 首重 + 3kg内/3-5kg/>5kg 三档续重）作为**版本化主数据**落库，不硬编码。
8. **区域来源 = 两者都支持**：调用方传了省份就用；没传则按客户历史订单省份分布推断，并**显式标注为推断值**，同时返回分布、加权平均运费与最贵/最便宜省份。前置依赖确定性的地址→省份解析（票 05）。
9. **京东仓储费 = 存储费不进礼包单位成本**（4元/方/天，按月对账，属期间费用）。**耗材（泡沫箱/冰袋/干冰）与订单生产费（出库费）、入库验收费计入礼包成本**——它们按件实付，与自有包材同性质。
   > 2026-08-25 用户把「京东出库费」明确列为必须建模的因子，本条前半（含出库费与耗材）**已确认**，⚠️ 标注撤销。存储费的排除维持不变。
   > 出库费档位（原包否 + 多品）：`首3件 1.6 元/单 + 续件 0.2 元/件`。注意「首3件」单位是**元/单**不是元/件。样例 8 件礼包 = `1.6 + 5×0.2 = 2.6 元`。
10. **Agent 形态 = 正反双向，分票，正向先行**。反向组合的每一步都调用正向引擎；正向不对，反向再聪明也是错的。
11. **方案落库 = 应用层做，Agent 不写**。Agent 只返回结构化方案，由调用方落「礼包方案草稿」走人工确认，路子同现有订单草稿/运单草稿。
12. **价格口径三个数已澄清**：`客户采购价`（用户的收入，样例 300）、`对外售价`（终端零售价，样例 400）、`落地成本`（本 effort 要算的）。毛利 = 客户采购价 − 落地成本。样例中 400 是客户的加价，不进本系统的毛利计算。

## Decisions so far

<!-- 每张已关闭票一行：标题（链接）+ 一句话结论。 -->

（尚无票关闭。）

## 用户点名的因子 → 落在哪张票

用户 2026-08-25 点名四个必须建模的因子，全部已有归属：

| 因子 | 落点 | 说明 |
|---|---|---|
| **库存** | 票 08 | 可用份数走木桶短板；快照时效与第三方库存缺口是该票的待决策点 |
| **偏好** | **票 13（销售偏好/主推策略）** + 票 10（入参）+ 票 12（目标函数） | 2026-08-25 澄清：指**自家经营策略**「当前季节主推 xxx」，不是客户历史偏好。调用方传的输入偏好归票 10/12 |
| **京东出库费** | 票 07（公式）+ 决策 9 | 订单生产费，档位见 `input/jd-b2c-cold-chain-price-table.md` |
| **快递费** | 票 04（规则调研）→ 票 06（主数据+计费重量）→ 票 09（区域推断） | 三票串联，规则未确认前一切运费数字都是估算 |

## Tickets

| ID | 票 | 类型 | 依赖 | 状态 |
|---|---|---|---|---|
| 01 | [商品成本档案数据模型](issues/01-cost-archive-data-model.md) | grilling (HITL) | — | open（前沿） |
| 02 | [包材、装箱方案与耗材主数据](issues/02-packaging-master-data.md) | grilling (HITL) | — | open（前沿） |
| 03 | [京东库存批次与效期接口能力](issues/03-jd-batch-expiry-capability.md) | research (AFK) | — | open（前沿） |
| 04 | [京东快递冷链运费口径与抛重规则](issues/04-jd-express-freight-rules.md) | research (AFK) | — | open（前沿） |
| 05 | [地址→省份解析与历史订单区域回填](issues/05-address-province-backfill.md) | task (AFK) | — | open（前沿） |
| 06 | [运费主数据与计费重量模型](issues/06-freight-master-data.md) | grilling (HITL) | 04 | blocked |
| 07 | [礼包成本引擎口径与分层公式](issues/07-bundle-cost-engine-spec.md) | grilling (HITL) | 01、02、06 | blocked |
| 08 | [临期与库存约束模型](issues/08-shelf-life-inventory-constraints.md) | grilling (HITL) | 01、03 | blocked |
| 09 | [客户区域画像与运费推断输出](issues/09-customer-region-profile.md) | grilling (HITL) | 05、06 | blocked |
| 10 | [MCP 工具面与调用契约](issues/10-mcp-tool-surface.md) | grilling (HITL) | 07 | blocked |
| 11 | [Excel 成本种子一次性导入](issues/11-excel-seed-import.md) | task (AFK) | 01、02 | blocked |
| 12 | [反向组合的约束集合与目标函数](issues/12-reverse-bundle-search.md) | grilling (HITL) | 07、08、13 | blocked |
| 13 | [销售偏好（主推策略）模型](issues/13-sales-push-strategy.md) | grilling (HITL) | — | open（前沿） |

## 依赖图

```text
01 商品成本档案数据模型 ──┬──> 07 礼包成本引擎口径 ──┬──> 10 MCP 工具面
02 包材与装箱方案主数据 ──┤                          │
04 京东快递运费调研 ──> 06 运费主数据与计费重量 ──┘   │
                                  │                  │
01 ─┬──> 08 临期与库存约束 ────────┼──────────────────┴──> 12 反向组合约束
03 京东批次效期调研 ┘             │                          ↑
05 地址→省份解析回填 ──> 09 客户区域画像 ──────────────┘      │
13 销售偏好（主推策略）─────────────────────────────────────┘
01 + 02 ──> 11 Excel 种子导入
```

## Not yet specified

<!-- 雾区：方向内但还无法精确成票的问题；前沿推进后逐块毕业为票。 -->

- **正向核算引擎的实现切分**（服务边界、缓存、精度与舍入策略）——票 07 定完口径再切。
- **反向组合的搜索算法**（穷举 / 贪心 / 约束求解）与各约束权重——票 12 定完约束集合再选。
- **账期比例与平台扣点是否进礼包落地成本**——`成品` sheet 有 `账期比例`/`账期费用/份`/`扣点`/`扣点费用/份` 四列，但它们随客户与渠道变化，不是商品固有属性；等票 07 分层时看是作为成本项还是作为报价参数。
- **客户历史商品偏好画像**——历史订单里除了发往哪儿，也留着「这个客户实际买什么品类/规格/价位带」。与票 09 同源、成本不高，但用户 2026-08-25 澄清「偏好」指的是销售偏好（票 13）而非此项，故不立票。等票 12 定目标函数时看要不要毕业。
- **动销速度的数据来源与口径**——临期评分要用，但现有 analytics 视图是发货量口径，是否够用等票 08 判断。
- **三类商品群成本列差异的统一方式**——经销品只有采购价/供货价/零售价，无加工构成；等票 01 定完深度再看是同表可空还是分表。
- **礼包方案草稿的表结构与人工确认流程**——决策 11 定了归属（应用层），形态等票 10 定完工具出参再说。
- **商品成本档案的维护页面**——导入之后靠什么维护，等票 11 落地后看。

## Out of scope

<!-- 已明确排除在本 effort 之外；永不毕业。 -->

- **京东仓储存储费的摊销核算**（4元/方/天）——决策 9，按月对账，不进礼包单位成本。
- **外部 ERP / 成本系统连接器**——决策 3，数据真源就是本系统 PG。
- **礼包整理 Agent 本身的 AgentDefinition 注册与运行时**——走 `.scratch/meta-agent-platform` 既有 Agent 平台，本 effort 只交付成本引擎与 MCP 只读工具面。
- **前端页面**——终点是 MCP 工具面可被调用；页面另起 effort。
- **礼包方案确认后的下单与履约**——现有 P0 Excel 闭环已覆盖。
- **财务对账、开票与税务处理**。
- **供应商侧采购价谈判与比价**（延续 `wayfinder/map.md` 的「采购部门内部流程」排除）。
