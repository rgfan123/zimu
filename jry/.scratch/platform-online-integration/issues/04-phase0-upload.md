> ## ⛔ 范围外（2026-08-18 合并裁决）
>
> 用户裁定 **Phase 0 不作交付**：不做脚本上传对接、cron、manifest 规范——那些是 Phase 1 上线后要扔的中间资产。三个 Python 脚本原样保留供人工兜底，但不投入工程化，也**不作为承诺的降级链**。
>
> 兜底 = **人工去平台导表 + 现有上传入口**（这条入口永在）。这同时采纳了红队 §2 的推翻结论：不维护「同一段契约双实现」的长期负债。
>
> 本票保留存档，不执行。
>
> **连带出范围**：D1（上传方式 A/B）与 D2（网关服务主体 `svc-platform-pull`）只服务 Phase 0，一并作废，不再需要用户确认。
# 04 — Phase 0 上传通道（ingest 文件进系统）

**What to build:** ingest 目录中的拉取文件自动进入系统导入批次闭环：脚本侧执行上传（幂等键 `imp-{channel}-{sha256}`），上传成功后在 manifest 标记已导入批次；重复上传不产生新批次。上传方式待用户确认：A) 脚本 curl 现有导入端点（需网关 Basic Auth 服务主体，D2）；B) 新增内部薄端点 + Bearer 服务身份（推荐，凭据不扩散到脚本层）。

**Blocked by:** 03

**Status:** wontfix

- [ ] ingest 文件可一键导入为系统批次，重复上传幂等
- [ ] 上传失败可重试且不重复建批次
- [ ] 网关/服务主体身份与操作人审计一致（D2 结论落地）
