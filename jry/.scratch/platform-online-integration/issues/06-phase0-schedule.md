> ## ⛔ 范围外（2026-08-18 合并裁决）
>
> 用户裁定 **Phase 0 不作交付**：不做脚本上传对接、cron、manifest 规范——那些是 Phase 1 上线后要扔的中间资产。三个 Python 脚本原样保留供人工兜底，但不投入工程化，也**不作为承诺的降级链**。
>
> 兜底 = **人工去平台导表 + 现有上传入口**（这条入口永在）。这同时采纳了红队 §2 的推翻结论：不维护「同一段契约双实现」的长期负债。
>
> 本票保留存档，不执行。
>
> **内核已转移**：调度/防重入/重试 → 新票 **13（调度与拉取节奏）**，按 Phase 1 `@Scheduled` 形态重做。
>
> **D3 已裁决**：告警只做**系统内可见**（`last_error` + `last_pull_at` + AuditLog + ConnectorsPage 健康态 + 工作台异常卡），不建企微/邮件外发通道——`connector/wecom/` 目前只收不发，建发送端是独立能力，与接入无关。10 的阻塞随之解除。
# 06 — Phase 0 调度与告警

**What to build:** 每日定时自动执行彩食鲜/飞象拉取+上传：cron 错峰（彩食鲜 08:30 / 飞象 08:50）、flock 防重入、通道内重试 2 次指数退避、状态文件记录每次运行结果；失败可发现——告警通道待用户确认（D3：当前系统无发消息能力，可先日志+状态文件+操作员看板，或接入企微发送）。

**Blocked by:** 05, D3 决策

**Status:** wontfix

- [ ] 定时任务按错峰表执行，flock 防重入生效
- [ ] 单次失败自动重试，最终失败写入状态文件并触发已确认的告警通道
- [ ] 连续运行 3 天无人工介入正常闭环（拉取→上传→状态更新）
