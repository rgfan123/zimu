# 05 — 履约闭环集成验证与真实探针

**What to build:** 把 00-04 的链路串成端到端可验证的整体：Mock 模式下全链路演示（SKU 核对 → 库存决策 → 建出库单 → 回填运单 → 状态推进），并扩展只读探针覆盖真实权限（addSoOrder/querySoOrder/queryGoodsInfo 是否已开通），依据结果更新京东外部 gate 记录——真实接口权限未开通前，闭环只以 Mock 演示，不冒充真实履约。

**Blocked by:** 01 — 京东云仓建出库单并落地本地 shipment, 02 — 京东出库单回填运单并推进履约状态, 03 — 库存决策接入京东实时库存, 04 — SKU 映射核对与差异告警

**Status:** wontfix

- [ ] Mock 全链路演示脚本/测试：从 SKU 核对到库存决策到建单到回填到状态推进，一条命令跑完且每一步结果可读。
- [ ] 探针扩展：覆盖 addSoOrder/querySoOrder/queryGoodsInfo 真实调用（受写模式门闩约束，默认不触网），输出成功/业务码/requestId/数据存在性，不输出凭据与 PII。
- [ ] 京东外部 gate 记录按探针结果更新：已开通 / 2001 未授权 / 缺参数，标注验证日期。
- [ ] 未配置凭据时探针明确拒绝并列出缺失键；探针文件不以 Test 结尾，普通测试套件不触发外部调用。

## Comments

- 2026-08-13：此票已被 `jd-fulfillment-loop/07 — 验证京东履约闭环与真实环境门禁` 取代。`addSoOrder` 是真实写操作，绝不属于只读探针；保留文件仅用于追溯，不得继续领取或实现。
