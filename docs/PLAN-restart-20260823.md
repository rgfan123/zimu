# 重启计划 — 原型形态还原（2026-08-23，整体决策记录）

本文件由整体决策会话产出，供任何接手会话直接执行。决策依据：ADR 0001（原型=形态契约）、
ADR 0002（全站统一外壳）、ADR 0003（预览栈晋级，8088 不动）、ADR 0004（外壳=原型壳层 CSS
1:1 移植 + 岗位重排不隐藏 + 真实徽标 + 诚实搜索入口；用户明确授权推翻首版 AntD 近似外壳后的
重做）、GitHub #103 spec 与 #104–#112 票面。

## 已确认的事实（不要重新调查）

- `/to-spec`、`/to-tickets` 已完成：#103 是 spec，#104–#112 是切好的 9 张票，全部 `ready-for-agent`，
  每张票已内嵌 Prototype fidelity 段并钉了原型 SHA-256。#95–#98 已实现并关闭（不是被取代）。
- 交接文档（HANDOFF-workbench-to-spec.md）§5 的五个悬置问题已全部被 spec 回答：
  D3（岗位存 localStorage、URL 优先）、D8（今日双口径、复核无时间边界、岗位与时间正交）、
  D9（复核清零后不自动确认）、Out-of-Scope（Phase 1 审计归属现状可接受；金额对账 Phase 3 另立票）。
- §6 两件要再开 /prototype 的事已取消：复核形态由 `ReviewCaseDrawer.tsx` 落地（D19）；
  岗位×时间不组合（D8）。
- 返工原因：此前实现未通过用户的视觉验收（"不像原型"）。因此本轮所有票的完成定义
  必须包含 ADR 0003 的视觉门禁。

## 执行顺序（每阶段：预览端口验收 → 晋级 8088）

| 阶段 | 票 | 内容 | 备注 |
|---|---|---|---|
| P0 外壳 | #104 | 侧栏 + 岗位下拉 + 删顶栏 + 全站换壳 | ADR 0002：旧页面进新壳即可，先不精修；菜单入口随后续路由落地逐步露出 |
| P1 发货线 | #107 → #108 → #109 | 同步按钮/配额 → 八段链路 → 整批确认/重试 | ⚠️ #107 晋级前置：后端平台拉取频控 TOCTOU 修复（见下方加固票 02）——工作台把刷新按钮做成第一屏主动作，会放大并发点击踩合规红线的概率 |
| P2 复核+调度 | #106、#105 | 收件箱岗位预筛；调度台消费后端 severity | 两票可并行 |
| P3 采购+对账 | #110、#111 → #112 | 采购台；对账台七态 + 证据链下钻 | #112 被 #111 阻塞 |

## 扫描产出的后端加固票（本地草稿：子牧/.scratch/scan-hardening-20260823/issues/）

01 HIGH `/demo/v1/*` 完全绕过网关鉴权（未授权任意写入 DEMO 域数据）
02 HIGH 平台拉取频控 check-then-act 竞态，可突破"每平台每日≤2次"合规红线（**#107 晋级前置**）
03 HIGH 中汇 PMS 批量上传：60s 全局幂等租约 + 无稳定外部引用对账，长批次可重复建外部商品
04 MEDIUM 中汇 PMS 缺独立 write-mode 开关（对照京东双重门禁模式补齐）

发布到 GitHub tracker 需用户确认后执行（gh issue create -R rgfan123/zimu）。

## 前端差距分析的三个票面修正（2026-08-23 扫描确认，实现时以此为准）

1. **频控「今日剩 N 次」不是零后端改动**：`ConnectorConfig` DTO 不含 `last_pull_at`，前端推导不出剩余额度。
   #107 首发按 ADR 0001 显示「配额状态未接入」诚实态；后端暴露字段并入加固票 02 的 B 部分。
2. **七指标中「仅报告未入库」只存在于 refresh 响应瞬时值**，刷新后无处可查；八段链路无按段聚合端点，
   且 ProcessingStage 八值与原型八段是两条不同轴——#108 实现时先定义映射，不可得的段位显示「暂无汇总」。
3. **全局搜索 ⌘K 无后端端点**（最接近的是 `orders?query=`）：外壳阶段做占位入口 + 「未接入」态，
   不伪造跨对象搜索。
4. 利好：`PageState.tsx`/`useAsync.ts`/`errorMessage` 三态基础设施已成体系、全站无假数据残留、
   `saasTheme.ts` 色板与原型同源——重构是壳层重建 + 视觉规约收紧 + 页面重排，不是错误处理重写。
   新工作台强制沿用 PageState/useAsync，禁止另起炉灶；禁止复制 DashboardPage 跨页拉全量反模式
   （fetchOpenReviewCases size=200 循环），计数用 size=1 取 total_elements。

## 范围裁决（2026-08-23 用户拍板）

- **深色模式：本轮不做。** 原型的 dark token 集（原型 :36-49）留在原型文件里作为将来素材，
  `saasTheme.ts` 本轮不加深色 token，侧栏 footer 不做深色切换齿轮。
- **窄屏/响应式：本轮不做。** 侧栏 <1080px 直接隐藏照抄原型即可，不做折叠态、不适配 iPad；
  响应式外壳仍归 #65，不并入本轮任何票。

## 并行施工分界（2026-08-23 划定：前端归本会话，后端归另一 Agent）

**前端会话独占（后端 Agent 不要碰）**：`frontend/` 全部、分支 `codex/issue-103-prototype-refactor`、
预览端口 18103、8088 前端镜像、#103–#112、agent-console #75–#80（是前端页面票）、
repo-design-hardening 前端族（#51–#53 契约生成、#57–#59 数据层、#61–#63 URL、#65–#69）。

**后端 Agent 可认领（按推荐顺序，2026-08-23 晚更新）**：
1. **#115 频控竞态 + 配额暴露**（最优先：A 是发货台晋级硬前置，B 是配额显示的数据源）
2. #119 发货台聚合计数端点（口径契约锁死，前端已按 size=1 先行不阻塞）
3. #114 / #116 / #117 加固批
4. **采购比价线（spec #120，ADR 0007–0010）**：#121 数据层 → #122 爬虫（Demo 源码在
   `data-local/procurement-demo/`，含密钥勿入库）→ #118 建议引擎+调度（已重写，前置线程池修复）；
   #123 工单商业化+回执凭证可与 #121 并行
5. 企微推送线：#82→#84；#87→#88；#89→#90；#86 独立（#81/#83/#85 已关，阻塞已清）
6. 聚福宝 P0：#99→#100/#101（#102 真实账号验收需人）
7. #24 MCP 闭环工具一期；之后 #113 source-sync（需先 to-tickets）

**前端会话新增认领**：#124 盯盘品界面（等 #121）、#125 工单价格/凭证 UI（等 #123）、
#110 按 ADR 0010 修正语义（评论已附）。

**协作规则**：后端票若需改 frontend/ 下文件，只在票里评论说明接口形状，由前端会话消费；
新增/变更 API 响应形状必须在对应票评论钉死后再动。合作票（#21–#23 复核信息补齐）后端只做
payload 半，渲染半留给前端会话。

## 环境约定

- 8088：真实栈，保持现状（ADR 0003）
- 18103：预览前端（本地 auth proxy 之后），基于 `codex/issue-103-prototype-refactor` 分支
- 原型基线：`/private/tmp/zimu-scan/zimu-frontend-prototype.html`（SHA-256 `adca4a89…`，与票面一致）
- frontend/ 在 iCloud 同步盘上，禁止递归 grep；文件已做过一轮实体化，见根目录卡顿修复记录
