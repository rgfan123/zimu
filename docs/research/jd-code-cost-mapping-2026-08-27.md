# 京东商品编号 + 产品成本核算：落表决策与映射结果

日期：2026-08-27
代码基线：`f6ed22551c287d8b019b10d2ae8058393250e910`（`feat(wecom): 会话管理——备注名/服务 Agent/回复权限一页收口`）
生产库快照：`zimupc` / `zimu-fulfillment-postgres-1` / `fulfillment_hub`
调研方式：**只读**。仅执行 `SELECT`；未执行任何 `INSERT/UPDATE/DELETE`，未改代码、未改 schema。
输入文件：
- `/Users/jerry/zimu-work/inbox/京东商品编号.xlsx`
- `/Users/jerry/zimu-work/inbox/A产品成本核算26.3.29.xlsx`

---

## 结论（先说结果）

**这批作业的净写入量是 0 条，且这是正确结果，不是失败。** 两份文件各自被一个独立的硬事实挡住：

1. **`京东商品编号.xlsx` 已经 100% 在库里了。** 该文件 SHA-256 与生产库 61 条 `provider_skus` 上记录的 `catalog_source_sha256` **逐字节相同**，也与仓库权威清单 `jd_source.sha256` 相同。63 行数据 / 61 个唯一 EMG 码 / 2 个重复码全部已落库。**没有一条新映射可加。**

2. **`A产品成本核算26.3.29.xlsx` 的 AJ 列不是成本，是 `售价`。** 任务书转述「用户明确：AJ 列是成本」，但文件表头 `AJ1` 字面就是 **`售价`**。真正的成本列是 M/N/AF/AI/AP/AQ。**把 AJ 写进 `purchase_price` 会把 27 条受治理的价格覆盖成语义不同的另一个数。**

外加一条制度性阻断：该成本文件**不是**仓库治理指定的价格源，而治理文档明令「任一指纹不同 → 停止生成，另起变更审查」。

成本数据已整理为 `/Users/jerry/zimu-work/inbox/成本待入库.csv`（110 行 × 全部 6 个成本列 + 候选 SKU + 阻断原因），留待人工判定后走正式变更流程。

---

## 1. 文件解剖

### 1.1 `京东商品编号.xlsx`

SHA-256 `85ca324d607c651117f660007893aee6c88ad1681a7625dde0176e88a5deb873`

| Sheet | 规模 | 内容 |
|---|---|---|
| Sheet1 | 65 行 × 10 列（63 行数据） | `彩食鲜商品 / 数量 / 聚福宝商品 / 数量 / JD / 编码` |
| Sheet2 | 202 行 | 礼包拆包组成（礼包名 → 成分 EMG + 件数） |
| Sheet3 | 23 行 | 易和天下参照表（EMG / 名称 / 件数 / 渠道 / 单品） |
| Sheet4 | 37 行 | 聚福宝参照表（同上） |

**「京东商品编号」到底是什么编号：是 `EMG` 开头的京东侧商品编码，即 `app.provider_skus.provider_sku_code`。** 三重互证，不是推断：

1. **文件自身**：Sheet1 的列名就叫 `编码`，值形如 `EMG4418727174451`；Sheet3/Sheet4 把同一批 EMG 码分别挂在「易和天下」「聚福宝」两个渠道下当参照。
2. **生产库形态**：`app.provider_skus` 中 `fulfillment_provider_id=1`（`JD` / 京东云仓）的 61 条 `provider_sku_code` 全部形如 `EMG44187…`，与文件取值域完全一致。
3. **前次调研结论**：`docs/research/zhonghui-sku-mapping-gap-2026-08-27.md` §1 已定性——`app.provider_skus` 是「京东侧编码（`EMG…`），出库阶段用，不参与来源解析」。

**排除项（不是这些）**：
- 不是渠道货号。中汇/彩食鲜的 `source_sku_ref` 是 `60043846` / `2056585` 这种 8 位或 7 位纯数字，落 `app.source_channel_skus`，与 EMG 取值域不重叠。
- 不是条码。国际条码在成本文件的 D 列（`国条`），形如 `06977872890357`，14 位纯数字，且 `app.skus.barcode` 目前 64 条全为空。
- 任务书提到的「P26 开头的渠道货号」在两份文件中**一次都没出现**，全库 `provider_sku_code` 也无 `P26` 前缀。

### 1.2 `A产品成本核算26.3.29.xlsx`

SHA-256 `e185b33fb5e856e9bdc324d6f4af8278ffb6937db3b09c4405f849208c2c86e4`

| Sheet | 规模 | 内容 |
|---|---|---|
| `成品` | 114 行 × 59 列（110 行有品名） | 自产成品成本核算 —— **本次成本数据所在** |
| `包材` | 6 行 | 包材单价（贴体盒/黑盒等），与 SKU 无关 |
| `送仓经销品` | 112 行 | 京诚乾元外部产品价格表，表头只到 R 列 |
| `一件代发经销品` | 76 行 | 乾元一件代发价格表，表头只到 Y 列 |

`成品` sheet 表头（逐列，经 `openpyxl` 显式坐标读取复核，行 1 无合并单元格）：

```
A 产品名称   B 产品状态   C 规格（g） D 国条     E 品牌     F 肉类     G 原料
H 供应渠道   I 包装形式   J 加工要求   K 净含量/g  L 加工规格/g
M 原料成本kg/元        N 核算成本/份    O 原料利润   P 成本+原料利润/kg
Q 人工费     R 人工占比   S 修割损耗率  T 损耗成本/KG U 损耗后成本/KG
V 加工后成本/KG        W 加工后成本/份
X 盒/袋  Y 贴纸/腰封  Z 膜  AA 签  AB 泡沫箱/纸箱+冰袋
AC 耗材/KG  AD 耗材/份  AE 耗材占比  AF 含耗材成本/份
AG 物流（原料进货）/kg   AH 物流（成品送货）/kg   AI 线下供货成本/份
AJ 售价     ← ★ 任务书说这是「成本」
AK （无表头）           AL 账期比例   AM 账期费用/份  AN 扣点   AO 扣点费用/份
AP 总成本/KG            AQ 扣完成本/份  AR 供货价   AS 毛利率  AT 促销价格  AU 大促
```

---

## 2. ★ AJ 列争议：文件表头写的是 `售价`，不是成本

这是本次作业最关键的一处分歧，必须由人来裁决，我不做替换。

**验证方式**（避免「数列数错」）：用 `openpyxl` 非 read-only 模式按**显式单元格坐标**读取，而不是靠位置下标推算：

```
AI1 = '线下供货成本/份'   AI2 = 8.8761888888889
AJ1 = '售价'              AJ2 = 12
AK1 = None                AK2 = 0.260317592592592
AP1 = '总成本/KG'         AP2 = 81.2113427350428
AQ1 = '扣完成本/份'       AQ2 = 10.5574745555556
AR1 = '供货价'            AR2 = 14.7
```

`ws.merged_cells` 在第 1 行为空 → 表头没有跨列合并，列字母与表头一一对应，不存在「视觉错位」。

**第二重证据：数值走向也否定「AJ = 成本」。** 每一行都满足 `AJ > AI`（售价 > 线下供货成本）、`AJ > AQ`（售价 > 扣完成本）。若 AJ 是成本，则成本恒大于成本，自相矛盾。

**第三重证据：AJ 与库内既有价格对不上任何固定倍率。** 取「品名可匹配 + 规格一致 + 库内已有价格」的 20 条做基准比对（`db_rp` = `app.skus.retail_price`，来自受治理的价格导出）：

| SKU | 品名 | db_pp | db_rp | 文件 AI 成本/份 | 文件 AJ 售价 | db_rp ÷ AJ |
|---|---|---:|---:|---:|---:|---:|
| SKU-JD-000011 | 新西兰羔羊羊颈排 | 98.00 | 116.00 | 58.39 | 78.00 | 1.487 |
| SKU-JD-000001 | 上脑肉片 | 106.50 | 158.00 | 73.02 | 85.00 | 1.859 |
| SKU-JD-000014 | 小龙坎火锅拼盘(6拼) | 99.00 | 152.25 | 56.25 | 66.90 | 2.276 |
| SKU-JD-000036 | 小龙坎火锅拼盘(4拼) | 58.00 | 108.00 | 35.95 | 39.90 | 2.707 |
| SKU-JD-000035 | 牛后腿肉 | 112.00 | 128.00 | 32.50 | 46.00 | 2.783 |
| SKU-JD-000022 | 牛腩块(500g) | 96.00 | 108.00 | 32.50 | 38.00 | 2.842 |
| SKU-JD-000002 | 羊小腿 | 68.00 | 99.00 | 23.47 | 32.00 | 3.094 |
| SKU-JD-000004 | 羊排块(羊寸排) | 86.00 | 126.00 | 29.85 | 41.00 | 3.073 |
| SKU-JD-000006 | 带骨羊后腿块 | 94.00 | 136.50 | 35.16 | 44.00 | 3.102 |
| SKU-JD-000013 | 羊肉馅 | 78.00 | 99.00 | 24.53 | 31.00 | 3.194 |
| SKU-JD-000030 | 牛肉馅 | 88.00 | 108.00 | 27.19 | 34.00 | 3.176 |
| SKU-JD-000008 | 羊肉块 | 116.00 | 158.00 | 40.48 | 49.00 | 3.224 |
| SKU-JD-000005 | 羊蝎子 | 68.00 | 78.00 | 19.21 | 24.00 | 3.250 |
| SKU-JD-000007 | 羊腿小切 | 106.00 | 139.50 | 21.34 | 42.00 | 3.321 |
| SKU-JD-000025 | 牛肋排 | 102.00 | 109.00 | 21.80 | 31.00 | 3.516 |
| SKU-JD-000026 | 筋头巴脑(500g) | 94.00 | 138.00 | 27.19 | 37.00 | 3.730 |
| SKU-JD-000003 | 带骨羊肉块 | 95.00 | 149.00 | 29.85 | 39.00 | 3.821 |
| SKU-JD-000015 | 羊棒骨 | 74.00 | 79.00 | 13.37 | 19.00 | 4.158 |
| SKU-JD-000012 | 新西兰羔羊肉卷 | 101.00 | 145.00 | 14.98 | 25.00 | 5.800 |
| SKU-JD-000024 | 牛肉饼(1.2kg) | 109.50 | 158.00 | 0.00 | （空） | — |

倍率在 **1.487 ~ 5.800** 之间散开，无任何固定系数（既不是 ×2 的「500g\*2 双袋」关系，也不是统一毛利率）。说明这份成本表与库内价格处在**两套完全不同的商业口径**上：成本表是「单份/线下供货」口径，库内是「京东渠道整售」口径。**两者之间不存在可推导的换算关系** —— 任何换算都会是猜测。

**若确认要入库，正确的列应当是**（按口径二选一，需业务确认）：
- `AI 线下供货成本/份`（含耗材 + 物流的全成本），或
- `AQ 扣完成本/份`（再叠加账期与扣点后的成本）。

`AJ 售价` 若要入库，对应的字段是 `retail_price` 而非 `purchase_price`，且同样需先解决下述口径与匹配问题。

---

## 3. 落表决策与依据

### 3.1 生产侧主数据现状（`information_schema` 实查）

| 表 | 成本/价格相关字段 | 现状 |
|---|---|---|
| `app.skus` | `purchase_price numeric`、`retail_price numeric` | **成本落点存在**。64 条中 29 条已填（27 条 JD + 2 条 TP），35 条为空 |
| `app.products` | `purchase_price`、`retail_price`、`other_cost` | 63 条中仅 2 条填了 pp/rp，`other_cost` 全空 |
| `app.provider_skus` | `provider_sku_code`、`merchant_sku_code`、`external_codes jsonb` | 63 条（JD 61 + TP 2） |
| `app.source_channel_skus` | `(source_channel, source_sku_ref)` 唯一键 | 75 条 |
| `app.skus.barcode` | `character varying` | **64 条全为空** |

→ **库里有成本字段，不缺 schema**。因此「缺 schema 留待迁移」这一分支不成立；阻断原因是语义与治理，不是结构。

### 3.2 决定性发现：`京东商品编号.xlsx` 已被权威清单固化并全量导入

仓库存在一条**运行时导入的唯一通道**，见 `docs/authoritative-jd-catalog.md` 与 `backend/src/main/resources/data/authoritative-jd-sku-catalog.json`：

> 仓库固化的 `authoritative-jd-sku-catalog.json` 是运行时导入的唯一输入。**不要在运行时直接解析 Excel，也不要从类似商品名自动补全价格。**
> JD 编码源固定 SHA-256 `85ca324d…`；价格源是 `合作商品价格查询导出_按商品名称去重.xlsx`，固定 SHA-256 `7fc1d34e…`。
> **任一指纹不同都表示源已变更：停止生成，另起变更审查，不得直接更换脚本中的指纹。**

指纹比对结果：

| 源 | 治理指定 SHA-256 | inbox 实际文件 | 判定 |
|---|---|---|---|
| JD 编码源 | `85ca324d…` | `京东商品编号.xlsx` = `85ca324d…` | **完全一致** |
| 价格源 | `7fc1d34e…`（`合作商品价格查询导出_按商品名称去重.xlsx`） | `A产品成本核算26.3.29.xlsx` = `e185b33f…` | **文件名与指纹均不同** |

生产库 61 条 JD `provider_skus` 的 `external_codes` 里逐条记着同一组指纹，例如：

```json
{"catalog_source": "京东商品编号.xlsx",
 "catalog_source_sha256": "85ca324d607c651117f660007893aee6c88ad1681a7625dde0176e88a5deb873",
 "price_source_sha256":   "7fc1d34e2217207abe108b97e3d02c21c4263558448c8352626f087656e45160",
 "catalog_manifest_sha256":"882e6bb6f9d822e9b9f21305ded02e581ce6cdcbcc2cb0508910bb4896eea68a"}
```

**逐项复核该文件已全量落库（用 inbox 文件重新解析，不信任清单自述）：**

| 校验项 | 从 inbox Excel 重算 | 清单 `expected` | 生产库实测 | 结果 |
|---|---|---|---|---|
| Sheet1 数据行 | 63 | 63 | — | ✅ |
| 唯一 EMG 码 | 61 | 61 | `provider_skus` where provider=1 → **61** | ✅ |
| 重复码 | 2（`EMG4418691848450`、`EMG4418727168035`） | 2 | — | ✅ |
| 码集合差异 | 与清单 keys **完全相等**（缺失 0、多余 0） | — | — | ✅ |
| 已定价 / 未定价 | — | 27 / 34 | `skus.purchase_price is not null` → 27 条 JD | ✅ |

→ **`京东商品编号.xlsx` 对 `provider_skus` / `skus` / `products` 的净可新增量 = 0。**

Sheet2/3/4 也已被清单显式列为 out-of-scope，并记录了排除理由：

```
Sheet2  179 非空行  BUNDLE_MAPPING_OUT_OF_SCOPE
Sheet3   20 非空行  CAISHIXIAN_REFERENCE_OUT_OF_SCOPE
Sheet4   37 非空行  JUFUBAO_REFERENCE_OUT_OF_SCOPE
```

Sheet2 是礼包拆包关系，对应 `app.source_channel_bundles`；但据前次调研，该表只服务 `DAZHE/WANGQI/WANQI` 三个渠道，与京东侧 EMG 码不是同一层概念，且清单已判定越界。**未动。**

### 3.3 落表决策汇总

| 目标表 | 决策 | 依据 |
|---|---|---|
| `app.provider_skus` | **不写** | 61/61 已存在且源文件指纹逐字节相同，净新增 0 |
| `app.skus`（主数据） | **不写** | 同上，61 条 JD SKU 齐备 |
| `app.products` | **不写** | 同上，63 条齐备 |
| `app.source_channel_skus` | **不写** | 两份文件均不含渠道货号（`60043846`/`2056585` 形态），无输入 |
| `app.source_channel_bundles` | **不写** | Sheet2 被权威清单判为 out-of-scope |
| `app.skus.purchase_price` | **不写** | AJ 是售价不是成本；且源非受治理价格源；且品名无法零猜测匹配（见 §4） |
| `app.skus.barcode` | **不写** | 成本文件 D 列有国条，但文件内条码本身有冲突（见 §4.3），且非本次任务范围 |

**WANGQI 渠道那 12 条 EMG 键的旧数据：全程未触碰，未出现在任何查询的 WHERE 之外，也未做任何写入。** 治理票在途，保持原状。

---

## 4. 匹配分析：为什么成本行无法零猜测地落到 SKU

即便先把 §2 的列语义问题放一边，品名匹配本身也过不了零猜测门槛。

匹配基准：把 `成品` sheet 的 110 个品名，与库内 64 条 SKU 的**规范名 + 全部别名**（别名来自 `provider_skus.external_codes.aliases`，即彩食鲜名与聚福宝名）做比对。

### 4.1 匹配分级结果

| 等级 | 条数 | 说明 |
|---|---:|---|
| `NAME_EXACT` 完全一致（仅做全半角/空格/星号归一） | **5** | 唯一可称「精确」的一档 |
| `NAME_LOOSE_UNIQUE` 需剥离「原切/精选/子牧/进口/澳洲/谷饲…」等修饰词后唯一 | 30 | 剥词本身即猜测 |
| `NAME_LOOSE_AMBIGUOUS` 剥词后仍命中多个 SKU | 2 | 歧义 |
| `NAME_FUZZY` 需 0.85 相似度模糊匹配 | 6 | 猜测 |
| `NO_CANDIDATE` 无任何候选 | **67** | 多为烧烤串品、研发品、鸵鸟/鸸鹋系列、窦清源品牌线，库内根本没有对应 SKU |

那 5 条 `NAME_EXACT` 是：`新西兰羔羊羊颈排`(row54→SKU-JD-000011)、`新西兰羔羊肉卷`(row55→…012)、`原切西冷牛排`(row70→…031)、`原切眼肉牛排`(row72→…032)、`肩胛烤肉片`(row74→…033)。

**但其中 2 条随即被「一 SKU 多行争抢」推翻**（见 4.2），实际可信的精确匹配只剩 3 条 —— 而这 3 条的成本口径问题依然未解。

### 4.2 一个 SKU 被多个成本行争抢（4 组 / 涉及 8 行）

这是最硬的反证：说明品名不足以定位唯一 SKU。

| SKU | 库内规格 | 争抢它的成本行 |
|---|---|---|
| `SKU-JD-000020` 上脑牛排 | 150g | row38 `澳洲谷饲上脑牛排` **800g**；row71 `原切上脑牛排` **150g** |
| `SKU-JD-000021` 牛肋条 | 500g | row61 `原切牛肋条` **500g**；row62 `原切牛肋条` **750g**（同名同条码，仅规格不同） |
| `SKU-JD-000031` 原切西冷牛排 | 150g | row40 `澳洲谷饲西冷牛排` **800g**；row70 `原切西冷牛排` **150g**（EXACT） |
| `SKU-JD-000032` 原切眼肉牛排 | 150g | row39 `澳洲谷饲眼肉牛排` **800g**（还同时命中 `SKU-JD-000055` 200眼肉牛排）；row72 `原切眼肉牛排` **150g**（EXACT） |

### 4.3 规格冲突与条码不可用

**规格冲突（3 条，候选唯一但克重对不上）：**

| 成本行 | 文件规格 | 候选 SKU | 库内规格 |
|---|---:|---|---|
| row27 `日式谷饲横膈膜` | 120g | `SKU-JD-000044` 日式谷饲横膈膜150g | 150g |
| row28 `韩式家庭烤肉` | 120g | `SKU-JD-000045` 韩式家庭烤肉150g | 150g |
| row33 `澳洲和牛牛肉饼` | 200g | `SKU-JD-000046` 和牛肉饼100g | 100g |

**库内规格未维护（3 条）：** row34→`SKU-JD-000048`、row69→`SKU-JD-000019`、row86→`SKU-JD-000053`，库内 `specification` 均为字面值 `待维护`，无法用规格佐证。

**条码不能当兜底键：**
- 库内 `app.skus.barcode` **64 条全为空**，没有可比对的一侧。
- 成本文件内部条码本身就有冲突：`06977872890579` 被 3 行共用（`原切精选羔羊肉卷`、`窦清源原切羔羊肉卷`、`窦清源精选羔羊肉卷`）、`06977872890432` 被 3 行共用、`06977872890135` 被 2 行共用（`原切牛肋条` 500g / 750g）。78 个条码只有 73 个唯一。
- 且任务纪律明令禁止由条码推导渠道货号。

### 4.4 跳过清单（按原因归类，共 110 行全部跳过）

| 阻断原因 | 命中行数 |
|---|---:|
| `AJ_IS_SELLING_PRICE_NOT_COST` —— AJ 表头是 `售价`，与任务前提冲突 | **110（全部）** |
| `SOURCE_NOT_GOVERNED_PRICE_EXPORT` —— 非治理指定价格源，指纹不符 | **110（全部）** |
| `NO_SKU_CANDIDATE` —— 库内无对应 SKU | 67 |
| `NAME_NOT_EXACT` —— 需剥词或模糊才匹配 | 36 |
| `SKU_CLAIMED_BY_MULTIPLE_COST_ROWS` —— 一 SKU 多行争抢 | 8 |
| `SPEC_MISMATCH` —— 规格冲突 | 6 |
| `AMBIGUOUS_MULTIPLE_SKUS` —— 剥词后仍多命中 | 2 |

（阻断原因可叠加，故合计大于 110。）

---

## 5. 成本处理结果

**未入库。** 库里有成本字段（`app.skus.purchase_price` / `retail_price`），所以不是缺 schema；阻断的是语义与治理：

1. AJ 列表头是 `售价` 而非成本 —— 直接与任务前提冲突，需人裁决（§2）；
2. 该文件不是治理指定的价格源，治理文档要求「停止生成，另起变更审查」（§3.2）；
3. 治理文档明令「不要从类似商品名自动补全价格」，而本次 110 行里最多只有 3 行能达到「精确且不冲突」（§4）；
4. 成本表口径（单份/线下供货）与库内价格口径（京东渠道整售）倍率散布 1.487~5.800，无可推导换算（§2）。

**已产出交接文件：`/Users/jerry/zimu-work/inbox/成本待入库.csv`**（110 行，UTF-8 BOM，Excel 可直接打开）

列结构：

```
sheet, excel_row, product_name, status, spec_g, barcode, brand, net_g,
M_raw_material_cost_per_kg          (M 原料成本kg/元)
N_calc_cost_per_portion             (N 核算成本/份)
AF_cost_incl_packaging_per_portion  (AF 含耗材成本/份)
AI_offline_supply_cost_per_portion  (AI 线下供货成本/份)
AJ_selling_price                    (AJ 售价 ← 任务书称的「成本」)
AP_total_cost_per_kg                (AP 总成本/KG)
AQ_cost_after_deduction_per_portion (AQ 扣完成本/份)
AR_supply_price                     (AR 供货价)
candidate_sku_code, candidate_sku_name, candidate_sku_spec,
match_level, spec_check, blocker
```

**六个成本列全部保留**，不替业务预选口径；`candidate_*` 与 `blocker` 列把每一行的匹配把握度和阻断原因摊开，供人工逐行判定。

所有列下标均以 `openpyxl` 显式坐标复核过（`AP2 = 81.2113427350428` 与坐标读取一致），排除数列错位。

---

## 6. 幂等与写入复核

本次**零写入**，故幂等无从谈起；改用「证明未改」的方式复核：

```sql
SELECT max(updated_at) FROM app.provider_skus;  -- 2026-08-21 10:51:29.390374+08
SELECT max(updated_at) FROM app.skus;           -- 2026-08-21 10:50:11.043445+08
```

两表最后更新时间仍停在 **2026-08-21**，本次作业日（2026-08-27）无任何行被触碰。

计数复核（作业前后一致）：

| 表 | 条数 |
|---|---:|
| `app.provider_skus` | 63（JD 61 + TP 2） |
| `app.skus` | 64 |
| `app.products` | 63 |
| `app.source_channel_skus` | 75 |
| `app.skus` 已填 `purchase_price` | 29 |

`app.source_channel_skus` 各渠道分布（未变）：`CAISHIXIAN 29 / DAZHE 12 / FEIXIANG 2 / WANGQI 12 / WECOM 6 / ZHONGHUI 14`。
**WANGQI 12 条全程未触碰。**

---

## 7. 建议的下一步（需人决策，不由本次作业代办）

1. **裁决 AJ 列语义。** 请提供成本的业务口径确认：要入库的是 `AI 线下供货成本/份`、`AQ 扣完成本/份`，还是别的列？若确实要用 `AJ 售价`，它应写入 `retail_price` 而非 `purchase_price`。
2. **确认口径换算。** 成本表按「份」（500g 单袋）计，库内 JD SKU 按「件」计且多数京东在售规格是 `500g*2`。入库前必须定死单位换算，否则会引入系统性 2 倍误差。
3. **走治理流程而非直接改库。** 按 `docs/authoritative-jd-catalog.md`，价格变更应重新生成 `authoritative-jd-sku-catalog.json` 并跑 `--check` + Catalog 真实 PostgreSQL API 测试；指纹变更需另起变更审查。直接 `UPDATE app.skus` 会让库内数据与仓库固化清单脱钩，下次导入被覆盖。
4. **67 行无候选品名**（烧烤串品、研发品、鸵鸟/鸸鹋、窦清源品牌线）若要纳入系统，是**新建 SKU** 的需求，不是补价格的需求，应单独立票。
5. **`app.skus.barcode` 全空** 是一个独立的主数据缺口。成本文件 D 列有国条可作来源，但需先解决文件内条码冲突（3 组共用条码），建议单独立票。
