# CONTEXT.md — 领域词汇表

订单履约与仓储物流中台（Fulfillment & Logistics Hub）的领域语言。仅收录术语与定义，不含实现细节。

## 核心术语

| 术语 | 定义 |
|---|---|
| **订单 Order** | 公司 B 端业务订单，从来源渠道进入系统后的统一业务实体。订单 ≠ 履约 ≠ 发货。 |
| **订单行 OrderLine** | 订单内的商品明细行（商品名称、规格、数量、单位）。 |
| **来源渠道 SourceChannel** | 订单进入系统的来源：`CAISHIXIAN`（彩食鲜）、`JUFUBAO`（聚福宝）、`FEIXIANG`（飞象）、`ZHONGHUI`（中汇）、`DAZHE`（大者）、`WANQI`（万齐）、`WECOM`（企业微信）；`WANGQI` 仅保留为历史技术兼容值。来源渠道与实际履约方分离；业务界面与文件只显示中文名称，不把内部技术键当作业务名。 |
| **来源订单标识 SourceOrderReference** | 来源平台可选提供的主单号或子单号，与系统内部订单编号、京东出库单号和快递单号均不同；缺失时由同一 Sheet 内连续的相同收货信息行块界定一个内部订单。 |
| **来源归因纠正 SourceAttributionCorrection** | 对已落库来源批次的渠道归因作出的追加式纠正。原批次、订单、原始行、文件、审计和幂等快照均不改写；读取、后续修订与来源回填使用最新有效归因。纠正、撤回纠正和回填失效都以新事实追加。 |
| **导入修订 ImportRevision** | 上传者明确声明为某个既有导入批次修正版的新批次；它保留原批次并形成版本链，不通过相似内容猜测或覆盖历史。 |
| **导入批次确认 ImportBatchConfirmation** | 操作员对一个来源订单导入批次作出的一次整体确认；确认覆盖该批次内全部已识别订单，不拆成逐行或逐客户确认，也不是新的批次状态。仍有 SKU、文件格式或数据质量阻断时不得确认；确认后才生成该批次的履约导出。 |
| **客户 Customer** | 公司内部跨系统共同维护的统一客户档案；一个客户可关联多个渠道身份、多个收货人和多个收货地址。来源订单文件中，规范化后的姓名与手机号二元组共同构成客户身份：两者同时相同才复用客户，任一不同均视为不同客户；首次出现时自动建立客户档案。消息中的客户描述不得直接替代客户身份。订单草稿未匹配客户时，授权内部员工可在复核中选择已有客户，或确认客户名称后创建新客户；新客户编码由系统生成，不由模型生成或操作员手工维护，创建后再绑定渠道身份。 |
| **收货人 Receiver** | 订单的收货信息（姓名、电话、地址），与客户（Customer）分离。 |
| **结账方式 SettlementMethod** | 订单的结账方式（如月结/现结/账期），结账时间 `settlement_time` 一并记录。真实万齐 52 列来源明确不提供二者时，内部以受限的缺失态保存，业务 API 展示为空；其他来源与人工入口仍必须提供。 |
| **商品 Product** | 对一组可履约规格的业务归类，例如“子牧羊小腿”；商品本身不承载包装规格、单位或履约方归属。 |
| **商品品类 ProductCategory** | 商品按主要肉类或用途形成的业务分类，与来源平台、履约方和包装规格分离。当前权威目录使用牛肉、羊肉、猪肉、禽肉、其他肉类、混合组合、设备物料与待分类；只有商品名称或已确认别名提供明确证据时才归入具体肉类，否则保持待分类。 |
| **商品毛利 ProductMargin** | 商品层零售价减去进货价与其他成本后的单位毛利额；只读派生值（零售价 − 进货价 − 其他成本），任一输入未填即视为未定价，不落库。 |
| **门禁 AgentGate / 运行期守卫 AgentGuard** | 平台对 Agent 定义与输入的两类行为约束：门禁（`AgentGateEngine`）在草稿不可确认时给出六项阻断（结构完整性 / 工具白名单合法性 / 只读不变式 / output_schema 可解析 / 凭据扫描 / 越权指令扫描）+ PII 警告（仅高亮）；运行期守卫（`AgentGuard`）默认链 [PII 拒绝] 在模型调用前对输入判定，命中 → outcome=REJECTED 转人工、不进模型，`guard_exemptions` 可豁免（默认空 = 生效）。守卫是行为约束，与 MCP 权限（访问控制，07）互不替代。 |
| **Agent 草稿 AgentDraft** | 受管 Agent 定义（`agent_definitions`）的 `draft` 状态行：由元 Agent（`meta-agent`）经 `create_agent_draft`/`update_agent_draft` 写工具创建/更新，全量快照 + 服务端校验 + 08 门禁，未经人工确认不可启用。与订单草稿（OrderDraft，客户订单语义）是不同概念；启用 = 确认（T11）。 |
| **商品标签 ProductTag** | 商品档案上的自由多选文本标签，去重存储；已用标签可作为候选复用。 |
| **商品主图 ProductMainImage** | 商品档案的单张主图，上传存储后以引用形式保存，可替换或清除。 |
| **商品原料 ProductIngredient** | 商品的原料/成分描述文本。 |
| **上市周期 ListingPeriod** | 商品的上架日期范围（起始 ~ 结束），可只填起始；起始不得晚于结束。 |
| **发货时效 ShippingLeadTime** | 商品下单后的发货时间承诺，以小时数记录并展示为「X小时内发货」。 |
| **SKU Sku** | 公司内部唯一的可履约商品身份，编号格式为 `SKU-{provider_code}-{6位全局流水号}`；每个 SKU 一期固定归属一个履约方，并通过来源渠道或履约方映射关联外部编码。第三方当前处理的猪肉等非牛羊肉商品通过 SKU 主数据显式配置履约方，不由意图模型根据商品名称或品类猜测；未匹配 SKU 必须进入复核事项。 |
| **定制礼包 CustomBundle** | 其他部门按客户需求随订单明确给出的动态商品组合；组件清单属于该订单的不可变快照，不是 SKU 主数据上的固定 BOM。它不需要加工或额外组装，只要求若干商品按清单完整配齐并同盒发出。履约单位是“完整礼包份数”：散件、缺件或拆开独立发货的组件不得计作已发礼包。 |
| **静态礼包 StaticBundle** | 以主数据形式维护的固定礼包 BOM（`product_bundles` + `bundle_items`）：礼包自身不创建 SKU、不单独计库存；来源礼包通过 `source_channel_bundles` 显式映射。订单命中后按履约方把当前 BOM 快照为一个或多个同质 `CUSTOM_BUNDLE` 订单行；每行仍只归属一个履约方，各行共同保留同一来源行血缘。任一分片未完成或待复核时，来源礼包都未完成且不得生成最终来源回填；主数据后续修改不改写历史订单。 |
| **履约 Fulfillment** | 跟踪一条订单行从待发货到实际发货的执行单元；一期始终由该订单行既定的单一履约方完成，缺货采购只是其中的补货分支。 |
| **履约方 FulfillmentProvider** | 实际负责备货与发货的主体，例如京东云仓或第三方；它与订单来源渠道分离，采购成功后仍回到原履约方。 |
| **发货进度 ShippingProgress** | 履约单元按实际发货数量呈现的结果层级：未发货、部分发货、已发货。 |
| **处理阶段 ProcessingStage** | 每条订单行在 P0 Excel 闭环中的权威当前位置，例如待复核、待生成发货表、等待履约方、已取得运单、待生成来源回填表、已完成或异常；订单本身只展示各行聚合后的进度摘要。 |
| **订单进度摘要 OrderProgressSummary** | 多行订单面向操作人员的聚合进度，取尚未完成订单行中的最慢阶段；它是下级真实状态的摘要，不是独立可推进的业务状态。 |
| **处理健康度 ProcessingHealth** | 与进度分离的操作提示，区分系统内部处理中、等待外部或人工动作、异常和全部完成；它用于表达当前责任与介入需求，不参与业务状态转移。 |
| **复核事项 ReviewCase** | 因订单草稿或履约方运单回传草稿待确认，或客户、SKU、数据质量及不支持业务条件而要求责任部门作出决定的人工待办；草稿复核发生在正式业务事实落库前，其他复核可阻断自动履约，均不能由模糊匹配自动关闭。企微一期每个订单草稿或运单草稿只创建一个责任团队为 `ORDER_OPS` 的开放复核事项，缺客户、缺 SKU 和缺字段均作为其中的检查项，不为同一草稿拆出额外的 `CUSTOMER_OPS` 或 `SKU_OPS` 待办。批量运单允许一次确认多条已通过校验的草稿；有问题的草稿继续保持开放，单行失败不得回滚其他已确认行。 |
| **运营提醒 OperationalAlert** | 不阻断自动流程、但要求相关人员知晓或确认的业务提示；例如库存不足时先部分发货并同时采购剩余数量。 |
| **部分发货 PartialShipment** | 订单行先发出当前可用数量、剩余缺口继续采购并后续再次发货的正常履约方式，不等同于履约异常。 |
| **实际发货数量 ActualShippedQuantity** | 数据中台中已实际发出的 Canonical SKU 件数：普通商品按来源包装乘数换算后的实发数量，定制礼包按实发完整份数展开各组件数量。它不是来源平台包装数、礼包份数或重量。 |
| **来源包装乘数 SourcePackMultiplier** | 一个来源平台销售单位包含多少件 Canonical SKU；来源商品名里的 `*N`（如 `500g*2`、`150g*4`）表达的正是它。它只描述来源平台如何组包销售，**不表达履约方的计数单位**。下单时按当时值冻结为订单行快照，此后修改主数据不改写历史订单。|
| **来源份数 SourceUnitQuantity** | 以来源平台销售单位计的数量，即来源表格里「下单数量」「发货数量」两列的口径。恒等式：`实际发货数量 = 来源份数 × 来源包装乘数`。由此，来源份数必为整数——履约方按整份来源销售单位出货，不存在半份。|
| **发货 Shipment** | 一次出库/发货批次，可包含同一订单、同一履约方、同一收货地址下的多条订单行。P0 默认一个 Shipment 使用一个出库单号并对应一个快递单号；缺货后的后续批次生成新的 Shipment、出库单号和运单。企微第三方运单回传不采集、不推断也不设置实际发货时间，因此已确认发货时 `shipped_at` 可以为空；系统另行记录运单接收与人工确认时间。P0 不把客户签收或妥投作为 Excel 闭环完成条件。 |
| **物流公司 Carrier** | 实际承运物流的标准主数据，以内部公司代码和名称标识。运单号前缀（例如 `SF`）可通过显式维护的前缀映射唯一命中物流公司候选；前缀规则属于确定性主数据，不写入模型提示词。未命中、多命中或与消息明示物流公司冲突时必须人工选择，一期唯一命中也仍需人工确认。 |
| **运单 Tracking** | 物流轨迹（物流公司 + 物流单号）。 |
| **履约方运单回传批次 ProviderTrackingBatch** | 单一履约方一次返回并原子接收的运单结果集合；京东与每个第三方各自成批，禁止在同一文件中混入其他履约方。 |
| **来源回填文件 SourceReturnExport** | 按订单来源平台的原始列格式生成、写入**来源份数**、快递公司与运单号的版本化文件。数量列一律用来源份数而非实际发货数量：来源平台按自己的销售单位记账，全部履约时回填的发货数量应与其下单数量相等（例如平台下单 1 组 `150g*4`，发往履约方 4 件，回填给平台的是 1）。数量必须是整数，不带小数位。包装乘数换算是履约方侧的事实，不对来源平台暴露；它可以汇集多个履约方的结果。同一来源商品行出现多个发货批次时，自动回填只写首批运单且不复制来源行，后续批次由复核事项交给人工跟进。 |
| **采购工单 ProcurementTicket** | 缺货时向采购部门发起的协同工单；它可以连续接收多个部分回执，直到缺口补齐、人工取消剩余量或失败后转人工处理。 |
| **履约结果 FulfillmentOutcome** | 履约结束时对请求数量的最终结论：全部履约、部分履约完成或全部取消；它与实际已发多少的 ShippingProgress 分离。 |
| **回传 Sync** | 发货结果按来源渠道回传（三平台 Connector / WECOM 模拟回传）。 |
| **订单事件 OrderEvent** | 订单时间线（Timeline）上的业务事件（ORDER_RECEIVED、JD_SHIPPED、SOURCE_SYNCED 等），与审计日志分离。 |
| **审计日志 AuditLog** | 接口调用与人工操作的审计记录（operator、request/response、trace_id），用于排错与追责。 |
| **异常 Exception** | 需要人工介入的订单状态分支（NEED_REVIEW、FULFILLMENT_EXCEPTION、SYNC_FAILED 等）。 |
| **演示场景 DemoScenario** | 与业务数据隔离的 Mock 演示运行，用于快速展示完整 Timeline；它不是 CanonicalOrder 内部闭环，不代表真实 Excel、履约方或来源回填已验收。 |
| **采购回执 ProcurementReceipt** | 采购部门对采购工单的一次不可变结果回传（SUCCESS / PARTIAL / FAILED + 本次可用数量 / 预计到货时间）；同一工单可有多个回执。 |
| **内部标准订单 CanonicalOrder** | 各来源表单归一化后长期保存在业务系统中的统一业务订单事实；京东导单表、第三方发货指令表和来源回填表都由它及后续真实履约结果派生，DemoScenario 不属于该闭环。 |
| **履约导出 FulfillmentExport** | 发货前从内部标准订单与履约数据生成、交给具体履约方使用的发货指令文件；一期包含京东模板与自定义第三方模板。文件一旦生成，相应订单行即形成履约承诺，后续修订不能覆盖原指令。 |
| **渠道消息 ChannelMessage** | 从企业微信等消息渠道接收到的原始业务证据；它只陈述谁在何处发送了什么，不等同于消息意图、订单草稿或内部标准订单。同一企微消息 ID 的重复回调属于传输重试，必须幂等地只保存和回执一次；不同消息 ID 即使内容相同也不得自动合并、删除或拒绝，只可在复核中提示疑似重复并由人工决定。系统只处理显式配置的企微业务群；群内企业成员均可提交，无需额外人员白名单，其他群的回调不得进入业务识别。订单或运单相关原始消息沿用业务订单审计保留周期；`NON_BUSINESS` 消息默认保留 30 天后清理，期限可配置。 |
| **消息媒体 MessageMedia** | 渠道消息携带的图片等原始附件。接收层必须及时下载、解密并将原件作为不可变业务证据保存；OCR 或多模态模型输出是记录模型版本的可重算派生数据，不得覆盖原件。图片下载、模型识别等异步任务发生临时错误时自动重试三次，仍失败才形成 `NEED_REVIEW` 意图及唯一的开放复核事项，不得因重试重复创建待办；人工复核始终可以查看原件并修正提取结果。订单或运单相关媒体沿用业务订单审计保留周期。 |
| **渠道身份 ChannelIdentity** | 外部渠道在特定企业、应用和能力作用域内提供的主体标识及资料快照。企微直接客户入口可提供 `external_userid` 与昵称等资料；“客户联系”场景还可能提供跟进员工维护的备注和描述。`external_userid` 不是客户原始微信号，且显示名、备注等可变、不可作为唯一身份。首次人工确认客户后，系统按企微主体、接入类型及 `external_userid` 保存其到唯一 Customer 的显式绑定；后续消息可以据此自动带出客户候选，但一期仍须人工确认订单。不同作用域的标识不得合并猜测，未绑定、零命中或多命中时进入复核事项。 |
| **转发消息 ForwardedMessage** | 授权内部员工从暂不支持自动接入的普通微信群转发到企业微信入口的渠道消息；转发员工与企微会话是可验证的传输来源，原微信发送人的昵称或描述仅作为证据，不作为客户或履约方的稳定身份。 |
| **内部运营人员 InternalOperator** | 系统内部运营人员的登记档案（`internal_operators`）：姓名、企微 userid、所属责任团队。`responsible_team` 由此可解析到具体人员列表与可推送 userid；未绑定企微 userid 的人员在需要推送时得到明确结构化提示，不静默跳过；停用（active=false）而非物理删除。只做映射与责任归属，不做登录/角色/权限；与渠道身份分离——`channel_identities` 是客户与群的外部身份证据，不是内部员工表。 |
| **企微业务通知 WecomBusinessNotification** | 复核事项、订单创建和发货完成产生的内部运营提醒。事实与源事务一起进入持久化 outbox，按责任团队和固定 5 分钟窗口汇总后，经 InternalOperator 映射推给已绑定人员；收件人以稳定 operatorId + userid 代际标识，停用/换组/换 userid 时旧重试必须收敛；未绑定/无人负责必须显式 BLOCKED，提交后结果不明必须 UNKNOWN 且禁止盲目重发。每次外呼前必须持有未过期批次租约；BLOCKED/FAILED/UNKNOWN 形成可重启恢复、稳定去重的持久运营告警，即使没有订单侧主体也不得只写日志。通知摘要只含判断所需业务标识，不复制客户 PII；真实分享路由落地前不拼伪链接。 |
| **消息提交 MessageSubmission** | 进入意图识别的一次显式业务提交。在企微群内，默认一条 `@机器人` 消息形成一个提交；后续文字或图片只有携带系统草稿号，或通道明确提供可验证的稳定父消息 ID 时才可追加到既有提交。当前企微引用类型/内容只作证据，系统不得据此猜测父消息，也不得按发送时间接近程度自动拼接群消息。 |
| **接收回执 MessageReceipt** | 群机器人在消息提交已安全保存后立即返回的固定回执，内容仅为“已接收”；它不等待图片下载、模型识别等异步处理，不暴露草稿号、客户、地址、商品或运单信息，也不代表意图识别、订单生成或运单关联已经成功。 |
| **消息意图 MessageIntent** | 根据一条或多条渠道消息得出的可重算分类结果，取值为 `CUSTOMER_ORDER`、`SUPPLIER_TRACKING`、`ORDER_CHANGE`、`ORDER_CANCEL`、`NON_BUSINESS` 或 `NEED_REVIEW`；它是派生判断，不修改渠道消息。`CUSTOMER_ORDER`、`SUPPLIER_TRACKING` 与 `NEED_REVIEW` 均创建相应的开放复核事项；`ORDER_CHANGE` 和 `ORDER_CANCEL` 只有携带合法订单号，或通道明确提供可验证的稳定父消息 ID 时才创建对应人工事项，否则归入 `NEED_REVIEW`。当前企微 quote 类型/内容不能充当父消息 ID；任何路径都禁止自动修改或取消已确认订单。明确判定为 `NON_BUSINESS` 的消息只保留原始证据与分类结果，不进入人工待办队列。 |
| **订单草稿 OrderDraft** | 从渠道消息提取出的、仍待补充或确认的结构化下单建议。模型只提取客户描述、收货人、电话、地址及商品名称、规格、数量等原始信息；Customer 与 SKU 候选均由现有确定性映射生成：唯一命中形成候选，零命中或多命中明确标记待复核，不得由模型猜测业务身份。同一客户、同一收货信息下的多个商品形成一个多行草稿；不同收货人或地址形成分别复核、分别成单的多个草稿，并共同引用原始消息，模型只提出拆分建议。一旦识别为客户订单，即使字段不完整也必须进入复核事项，并明确列出缺失字段。WECOM 来源单号由系统按消息提交与草稿序号生成，商品名称、规格和单位以人工确认后的 SKU 主数据为准；缺失的收货或结账信息由复核人员补充或选择已有资料。授权内部员工可以修订草稿，但模型原值、渠道资料快照、拆分建议、映射候选、人工修改和确认版本必须可追溯。它不是内部标准订单；所有标准订单必填项完整且经授权内部员工确认后才能转为内部标准订单或进入履约，确认前即使 Customer 或 SKU 唯一命中也不得自动成单。 |
| **履约方运单回传草稿 ProviderTrackingDraft** | 从渠道消息逐行提取出的收货人姓名、物流公司、快递单号与可选系统发货任务号；批量回传必须保持每行姓名与运单号一一对应，一行生成一个独立草稿与开放复核事项，多个草稿可共同引用同一原始消息。消息未明确提供物流公司时，可用已配置的运单前缀映射生成物流公司候选，但不得由模型或未配置规则猜测。发货任务优先按明确且可验证的系统任务号关联；缺少任务号时，可以把姓名中的 `*` 作为通配符，在当前待回传的第三方发货任务中按收货人快照执行确定性匹配，且只有实际恰好命中一个任务时才形成候选。零命中或多命中时必须由授权内部员工选择，不得根据转发人、群聊、昵称、时间接近程度或其他相似内容猜测。第三方只返回姓名与运单号时，表示该任务的全部商品和数量已经发出；部分发货、缺货或其他异常必须在消息中明确说明，并由人工按实际数量复核。企微回传不从消息提取或推断实际发货时间，原始消息发送时间只作为渠道证据时间。物流公司、快递单号、发货任务和实际发货数量均经人工确认后才形成正式运单事实。 |

## 状态维度

状态按维度分离维护，不合并进单一 `order.status`（完整转移矩阵后续需按数据库设计票 Q7 同步修订）：

- **OrderStatus**：主线 RECEIVED → VALIDATED → SKU_MAPPED → FULFILLING → SHIPPED → SYNCED → CLOSED；异常分支 NEED_REVIEW / OUT_OF_STOCK / PROCUREMENT_PENDING / FULFILLMENT_EXCEPTION / SYNC_FAILED / CANCELLED；demo 自动最终态 = SYNCED（CLOSED 不自动进入）
- **ShippingProgress**：NOT_SHIPPED / PARTIALLY_SHIPPED / SHIPPED；按实际发货数量计算
- **ProcessingStage**：每条 OrderLine 独立保存 NEED_REVIEW / READY_TO_EXPORT / WAITING_PROVIDER / TRACKING_RECEIVED / RETURN_FILE_READY / COMPLETED / EXCEPTION；采购分支另有 PROCUREMENT_IN_PROGRESS
- **ShipmentStatus**：P0 使用 CREATED → SHIPPED，或 CREATED → FAILED；DELIVERED 仅为未来接入京东 SDK/物流轨迹后的终态，不参与当前 Excel 闭环完成判定
- **SyncStatus**：PENDING → SYNCED；SYNC_FAILED →（重试）→ SYNCED
- **ProcurementStatus**：PENDING → SUCCESS / PARTIAL / FAILED；订单取消 → CANCELLED
- **FulfillmentOutcome**：IN_PROGRESS → FULLY_FULFILLED / PARTIALLY_FULFILLED / CANCELLED
- 多行订单：行级独立推进；订单进度摘要取最差进度，处理健康度另行聚合

## 边界

- 本系统不负责：普通个人微信群自动监听、企业微信平台运维、LangBot 部署、LLM/OCR 模型训练、采购内部流程、京东 WMS 库存算法；本系统提供 Agent 决策/调度层，一期 Agent 仅做只读分析与建议，任何业务写操作仍必须经授权人工确认；采购、意图识别、数据查询等 Agent 职责由代码定义并通过 MCP 只读工具执行；业务 Agent 写工具零调用，meta-agent 是唯一例外且受 allow_write + 门禁 + target-scope 三重约束；并负责已配置企微 API 机器人的消息接入、业务解释、草稿与人工复核闭环。
- 外部系统一律通过接口接入，**Connector 禁止直接写业务表**。

## 企业微信一期实现边界

- 不建设独立 WeCom Gateway，也不引入 LangBot；在现有 Spring Boot 单体内增加薄 `wecom` Adapter，负责回调验签解密、原始消息落库、媒体下载任务创建和固定回执。
- 接收事务只保存消息事实与 PostgreSQL 异步任务；下载、识别和重试由 Spring Worker 执行，不引入 Kafka 或 RabbitMQ。
- 图片一期复用内容寻址文件存储并使用持久化 Docker 卷；业务代码通过存储接缝访问，后续可替换为 MinIO/S3。
- 模型通过内部 `MessageInterpreter` 接缝调用；供应商、模型名称和提示词版本配置化并随派生结果保存。更换模型不得影响消息接收、草稿、复核或成单用例。
- 人工处理扩展现有复核工作台，展示原始消息与图片、模型原值、映射候选、人工修改和确认动作；不建设独立企微工作台。
- MCP Adapter 与 REST/UI 共用应用层用例。**stdio 面一期收紧为只读**（08 决策）：外部客户端共用全局 Agent 身份、无 per-agent 权限，`tools/list` 只暴露查询工具，写工具（触发重新识别、提交草稿修改建议、补充材料、进入人工复核、确认订单、提交京东建单）一律不对外；写操作只经 **Agent 面**（`AgentToolInvoker`）执行，且须白名单显式放行（`allow_write=true`）；不提供确认订单、确认运单、关闭复核事项或取消订单工具。
- 识别任务积压或最终失败只在管理后台展示并告警，不向原企微群补发处理结果；群内回执始终只有“已接收”。
